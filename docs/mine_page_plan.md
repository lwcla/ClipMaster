状态：已完成

# 我的页方案

## 当前状态

我的页是首页底部 Tab 的一级页面，依次展示权限项、备份与恢复、下载记录、折叠数据、回收站和检查更新入口；当 `:feature:magnet` 通过 `-PenableMagnetFeature=true` 编译进应用时，页面通过 `MagnetFeatureEntry.MineEntry` 追加磁力搜索入口，完整磁力模块化规则以 `docs/magnet_search_plan.md` 为准。磁力搜索、下载记录、折叠数据和回收站只在入口处展示轻量说明或统计，不在我的页同步源索引、执行搜索或加载对应分页数据。检查更新入口只负责触发轻量版本检查、展示结果和打开外部发布链接，完整自更新规则以 `docs/app_update_plan.md` 为准。本轮 UI 刷新后，我的页通过 `TopLevelPageScaffold` 接入统一一级页骨架，并按“权限”“数据管理”“功能入口”“设置”分组展示入口。

本文记录我的页自身的入口、设置项和页面级交互；首页 Pager、底部 Tab 和一级标题详见 `docs/main_page_plan.md`，共享剪贴 item 的斜向快捷动作区、右滑菜单和回调规则详见 `docs/clip_result_list_plan.md`。

当前代码组织已将我的页入口卡片、剪贴快捷动作设置弹窗和权限设置项适配层拆到同 package 组件文件中；`MinePage.kt` 保留页面状态收集、权限动作消费、生命周期刷新和系统跳转，避免页面入口继续混合所有 UI 细节。入口卡片、入口式开关卡片、单选弹窗、固定设置开关列表和设置开关行的完整共享规则以 `docs/shared_form_components_plan.md` 为准，本文只记录我的页接入方式和快捷动作业务差异。

“剪贴保存过滤”是我的页设置分组中的全局规则入口，完整规则、`QUERY_ALL_PACKAGES` 本地安装应用读取、图标懒加载、备份恢复和保存链路边界以 `docs/clip_source_app_filter_plan.md` 为准；本文只记录我的页如何接入口卡片并导航到独立设置页。

## 目标

- 保持我的页作为管理入口和偏好设置入口，避免在首页列表里堆叠全局设置。
- 新增“剪贴快捷操作”设置，让用户理解普通列表和普通搜索结果 item 可以通过斜向快捷动作区执行所选动作。
- 设置快捷动作时提供复制、置顶、删除、折叠、无（整卡进入详情）五个互斥选项，并在选择后立即影响普通列表和普通搜索结果。
- 新增“剪贴保存过滤”入口，让用户集中管理哪些来源 App 的后续剪贴不保存，同时明确历史记录不受影响。

## 用户体验

- 我的页继续使用单列卡片入口，顶部显示固定标题“我的”，入口按权限、数据管理、功能入口、设置分组。
- 权限分组固定放在我的页最上面，只展示 Shizuku 和通知两个权限项；两个权限项各自独立成卡，方便用户先看到影响剪贴读取和提醒展示的系统能力状态。
- 备份与恢复、下载记录、折叠数据、回收站以及可选磁力搜索这类页面导航入口只保留卡片自身点击反馈，页面切换动画统一交给 `AppNavigation` 的 Compose Navigation 官方左进右出转场。
- 磁力搜索入口只在磁力模块启用时出现，并且只负责进入磁力搜索页；Academic Torrents 索引状态、同步动作、搜索历史和结果分页都在磁力搜索页按生命周期启动。
- 检查更新入口展示当前 App 版本，点击后执行手动版本检查；Gitee 与 GitHub 更新源均不可达时展示“暂时无法检查更新，可稍后重试或前往发布页查看”，并提供可配置发布页入口。
- “剪贴快捷操作”作为普通设置行展示当前选项摘要，例如“当前：复制”。
- 点击该设置行打开单选弹窗，选项顺序为复制、置顶、删除、折叠、无（整卡进入详情）。
- 弹窗说明普通列表和普通搜索结果 item 的快捷动作区执行所选动作，其余区域进入详情；选择“无（整卡进入详情）”会关闭快捷动作区。
- 该设置是五选一偏好，不使用开关，避免把互斥动作误表达为启用/停用。
- “剪贴快捷操作”设置行只打开本页弹窗，不参与页面级导航转场。
- “权限”分组不再提供“权限说明”标题行、收起/展开按钮或展开动画。
- Shizuku 和通知权限不放在同一个卡片里，每个权限项都单独使用一张卡片，和其他分组入口保持一致的扫描节奏。
- 权限单项卡片内部展示权限语义图标、短标题、完整说明文案和右侧开关；短标题用于和其他入口卡片保持一致的扫描节奏，说明文案继续承载权限影响边界。
- Shizuku 和通知权限项的说明文案必须完整换行显示，不使用两行省略，避免用户看不到通知关闭后“剪贴仍会保存”等关键说明。
- 通知权限项只表达剪贴保存、下载进度、下载结果和 Shizuku 状态提醒的展示能力；关闭通知不影响剪贴入库。
- “剪贴保存过滤”作为设置行展示未设置或已过滤数量，点击后进入独立二级选择页。
- 过滤选择页展示 App 图标、App 名和包名，默认展示可启动非系统 App、历史来源和已过滤包名；系统 App 默认隐藏但搜索可命中，并提供“显示系统应用”开关。
- 本地安装应用读取失败或未读到应用时，选择页仍展示历史来源和已保存包名，允许手动添加、取消勾选、清空草稿和重新读取应用列表。
- 手动添加和清空全部都在页面草稿内完成，用户点击确定后才统一保存过滤名单。

## 数据和接口

- 快捷动作保存到 `AppSetting.clipItemQuickAction`，默认值为复制。
- 我的页 ViewModel 只负责读取和写入设置，不直接操作剪贴列表数据。
- 普通列表页和普通搜索页观察同一设置状态并传给 `ClipResultList`；折叠列表、折叠搜索和回收站忽略该设置。
- 后续新增剪贴 item 快捷动作时，需要同步扩展设置枚举、字符串资源、单选弹窗选项、共享 item 动作映射和本文档。
- 来源过滤名单读取 `AppSetting.blockedClipSourcePackagesFlow`，候选合并当前 `PackageManager` 直读安装应用、`source_apps` 历史来源和已过滤包名；安装应用读取结果、读取状态和当前安装图标不持久化、不进入 WebDAV/本地备份，搜索页来源筛选仍只读取 `source_apps`。
- 来源过滤名单保存后标记 backup dirty 并调度自动备份；具体备份字段和恢复并集策略见 `docs/clip_source_app_filter_plan.md` 与 `docs/webdav_backup_plan.md`。
- 检查更新使用 app 模块内的自更新协作者；我的页只持有入口 UI state、触发检查和分发外部链接打开事件，不承载 manifest 解析、版本比较或下载源排序逻辑。
- Shizuku 和通知权限项只使用页面内瞬时状态，不保存“权限说明是否展开”的 MMKV 偏好；历史 `permission_expanded` key 已废弃且不进入备份恢复协议。

## 涉及文件

- `app/src/main/java/com/cla/clip/master/ui/page/mine/MinePage.kt`
- `app/src/main/java/com/cla/clip/master/ui/page/mine/MineEntries.kt`
- `app/src/main/java/com/cla/clip/master/ui/page/mine/MineSettingComponents.kt`
- `app/src/main/java/com/cla/clip/master/ui/page/mine/MineVm.kt`
- `feature/magnet-api/src/main/java/com/cla/clip/feature/magnet/api/MagnetFeatureEntry.kt`
- `base/general/src/main/java/com/cla/clip/base/general/config/AppSetting.kt`
- `base/general/src/main/res/values/strings.xml`
- `docs/mine_page_plan.md`
- `docs/app_update_plan.md`
- `docs/clip_source_app_filter_plan.md`

## 测试验证

- 切换到我的页后，最上方应先显示“权限”分组，分组下只有 Shizuku 和通知两个权限项，且两个权限项应分别展示为独立卡片。
- 权限单项卡片内部应展示权限语义图标、Shizuku/通知短标题、完整说明文案和对应开关。
- Shizuku 和通知两个权限项的说明文案应完整显示；放大字体下也不能出现省略号截断关键权限说明。
- 设置行展示当前选项，点击后弹出单选弹窗。
- 单选弹窗顺序为复制、置顶、删除、折叠、无（整卡进入详情）。
- 选择任一动作后弹窗关闭，设置行摘要立即更新。
- 切回普通列表和普通搜索后，快捷动作立即生效；重启应用后设置保留。
- 选择“无（整卡进入详情）”后普通列表和普通搜索不显示斜向背景，整卡点击进入详情。
- 点击下载记录、折叠数据和回收站入口时应正常进入目标页面，并由 `AppNavigation` 呈现左进右出页面切换。
- 启用磁力模块时，点击磁力搜索入口应进入磁力搜索页；默认禁用构建不展示该入口。我的页不应提前下载源索引或读取磁力记录分页。
- 点击“剪贴快捷操作”设置行应立即打开弹窗，不触发页面级导航转场。
- 点击“检查更新”入口时应触发手动检查；无更新、有更新、Gitee 优先、GitHub 兜底和发布页入口均按 `docs/app_update_plan.md` 验证。
- “设置”分组下只展示剪贴快捷操作，不再混入 Shizuku 或通知权限项。
- 点击“剪贴保存过滤”入口应进入独立二级选择页；搜索、多选、显示系统应用、手动添加、清空确认、重新读取应用列表和可见行懒加载图标均可用。
- 本地安装应用读取失败时，已保存包名仍可显示和取消勾选；手动添加成功后进入草稿，返回不确认不应落盘。

## 已知取舍

- 设置只影响普通列表和普通搜索结果，不影响折叠列表、折叠搜索和回收站，避免“折叠”在不同页面产生反向语义。
- 删除动作只触发现有删除选择弹窗，不直接静默删除，避免误触造成不可恢复风险。

## 开放问题

- 后续是否需要为右半区动作也提供自定义设置暂不确定；当前右半区固定进入详情，降低学习成本。
- 来源过滤选择页暂不公共化为共享组件，因为它绑定本地安装应用读取、历史来源、手动包名校验和全局设置保存；若后续其他页面需要类似 App 多选，再评估抽出共享 App 选择页面骨架。

## 本次同步

- 我的页入口卡片和权限设置项列表已接入 `ClipMasterCard` 公共内容卡片外壳；外边距和页面入口语义仍由本页面维护，公共外壳只负责圆角、阴影、边框和触摸裁剪规则，完整规则详见 `docs/shared_card_component_plan.md`。磁力入口卡片由 `:feature:magnet` 自己渲染，宿主只提供打开动作。
- 我的页入口项复用 `ListEntryCard`；快捷动作设置弹窗复用 `SingleChoiceDialog`；权限项复用 `SettingSwitchEntryCard`，分别渲染 Shizuku 与通知两张入口式开关卡片。`SettingSwitchListCard` 继续作为表单式多项设置列表保留在共享组件中。本页面保留权限动作消费、系统跳转、Shizuku 回调监听和快捷动作枚举到资源文案的映射。
- 自更新能力以 `docs/app_update_plan.md` 作为主文档；我的页只记录入口展示、用户触发和页面可见时的轻量检查边界，避免重复维护 GitHub/Gitee 发布契约。

## 日志与诊断计划

- 本轮只调整我的页权限单项卡片的入口式视觉表达，不新增权限请求、系统跳转、数据库写入、网络、文件 IO 或后台任务副作用。
- 权限真实状态刷新和点击动作继续沿用 `MineVm.refreshPermissionStatus()`、`permissionActions` 和既有低敏日志；不新增运行时日志，避免静态 UI 排序产生噪声。
- 来源过滤设置新增低敏日志：手动添加草稿、拒绝当前 App、本地安装应用读取失败、列表为空和当前安装图标加载失败只记录 reasonCode、数量和耗时摘要，不记录完整 App 列表、图标内容或剪贴正文；完整日志计划见 `docs/clip_source_app_filter_plan.md`。

## 变更记录

- 2026-06-29：来源过滤选择页改为主进程 `QUERY_ALL_PACKAGES` + `PackageManager` 直读安装应用，并按可见行懒加载当前安装图标；原因是旧安装缓存同步方案未发布且实现复杂，当前自用/私有分发可以接受包可见性权限。
- 2026-06-17：废弃前方案曾让来源过滤选择页观察 `installed_app_cache`，并由 Shizuku 通过 Provider 同步安装应用元数据和图标；本轮已删除可执行链路，仅保留为历史取舍记录。
- 2026-06-17：新增“剪贴保存过滤”设置入口和独立二级选择页接入说明，并补充 App 图标 + App 名展示和页面草稿保存边界；原因是用户需要从我的页集中管理按来源 App 屏蔽后续剪贴保存，且只看包名难以识别 App。
- 2026-06-08：将 Shizuku 与通知权限卡片改为“图标 + 短标题 + 完整说明 + 右侧开关”的入口式开关卡片；原因是权限区需要和数据管理、功能入口、设置入口保持同一卡片扫描节奏。
- 2026-06-05：将 Shizuku 与通知权限从同一个权限卡片拆成两个独立卡片；原因是权限项需要像其他分类入口一样逐项成卡，提升扫描一致性。
- 2026-06-05：将权限拆成独立分组并放到我的页最上方，原“设置与权限”分组改为只承载普通设置；原因是用户希望先看到权限状态，且权限项已经固定为 Shizuku 和通知两项。
- 2026-06-05：移除权限卡片内部的 Shizuku/通知单项小标题，保留说明和开关；原因是外层已有“权限”分类，卡片内部不需要重复标题层级。
- 2026-06-05：权限项说明改为完整换行显示，不再两行省略；原因是权限说明包含“通知关闭不影响剪贴保存”等关键取舍，不能被截断。
- 2026-06-05：将“设置与权限”中的权限说明从可展开卡片改为固定展示 Shizuku 和通知两条权限项，并废弃 `permission_expanded` UI 偏好；原因是剪贴读取不再依赖悬浮窗/前台服务旧链路，页面不需要收起/展开动画。
- 2026-05-27：我的页接入 `TopLevelPageScaffold` 并按数据管理、功能入口、设置与权限分组；原因是全量 UI 刷新需要提升管理入口扫描效率，并统一一级页骨架。
- 2026-05-25：磁力搜索入口改为通过 `MagnetFeatureEntry.MineEntry` 可选接入；原因是磁力搜索已独立为编译期可选模块，默认构建不能展示磁力入口或保留磁力实现引用。
- 2026-05-24：补充检查更新入口接入摘要，并引用 `docs/app_update_plan.md` 作为自更新主文档；原因是自更新能力跨发布契约、网络、日志和 Mine 页 UI，不应把完整规则重复维护在我的页文档中。
- 2026-05-22：我的页新增磁力搜索入口，并记录入口不加载重型数据的约束；原因是磁力搜索第一版由独立页面承载同步和搜索，我的页只作为轻量导航入口。
- 2026-05-17：我的页入口卡片和权限说明可展开卡片接入 `ClipMasterCard` 公共内容卡片外壳；原因是应用内主要内容卡片需要统一沿用我的页面视觉，并集中维护圆角触摸反馈裁剪规则。
- 2026-05-18：我的页入口卡片、快捷动作单选弹窗和权限设置展开卡片接入共享表单组件；原因是这些组件不再只服务我的页，页面目录应只保留导航、权限和设置保存等强业务适配。
- 2026-05-16：新增我的页方案文档，并记录剪贴列表左半区动作设置；原因是该设置从我的页统一配置，且会影响普通列表和普通搜索结果的 item 点击行为。
- 2026-05-17：将设置说明从左半区动作更新为剪贴快捷操作；原因是共享 item 已改为左下斜向快捷动作区，需要同步用户可见文案、`AppSetting` 命名和页面说明。
- 2026-05-17：为下载记录、折叠数据和回收站入口补充元素联动导航反馈，并排除设置弹窗入口；原因是导航层过渡会带来旧页面触摸命中问题，入口自身反馈可以保留页面打开感且不影响本页设置操作。
- 2026-05-17：移除我的页入口元素联动反馈，恢复入口即时导航并交由 `AppNavigation` 官方左进右出转场呈现；原因是元素缩放观感较差，需要试验 Compose Navigation 原生页面切换能力。
- 2026-05-18：按当前代码组织规则拆分我的页 UI，新增入口卡片组件和设置组件文件；原因是页面入口应聚焦权限动作、生命周期刷新和导航编排，通用入口卡片、快捷动作弹窗、可展开设置卡片和开关行应放在更可预测的位置。
- 2026-05-26：检查更新入口说明同步为 Gitee 优先、GitHub 兜底；原因是自更新主方案已去掉网盘并改为双 release 源。
