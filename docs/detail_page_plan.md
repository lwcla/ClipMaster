状态：已完成

# 详情页方案

## 当前状态

详情页按路由传入的 `clipId` 加载单条剪贴记录，展示正文内容，并提供复制、删除、图片提取和视频提取入口。页面入口 `DetailPage` 负责触发加载、收集详情状态、处理删除成功后返回和接入统一删除选择弹窗；详情正文、加载态和错误态已接入 `ClipMasterCard` 公共内容卡片外壳，完整卡片规则以 `docs/shared_card_component_plan.md` 为准。

## 目标

- 保持详情页作为单条剪贴记录的阅读和操作页面。
- 保持删除、复制、图片提取和视频提取的既有行为不变。
- 普通内容卡片复用公共卡片外壳，避免详情页继续手写独立 `Card` 视觉。

## 范围

- `DetailPage.kt` 的加载态、错误态和正文内容卡片。
- `ClipDeleteChoiceDialog` 的删除选择语义保持由现有共享弹窗维护。
- 链接提示、底部按钮、正文滚动、复制和导航仍由详情页维护，不下沉到公共卡片组件。

## 用户体验

- 页面标题仍显示“剪贴详情”。
- 加载中、错误和正文内容都使用统一圆角、阴影和描边外壳。
- 正文长内容仍在卡片内纵向滚动，底部操作按钮保持在页面底部。
- 如果剪贴内容包含链接，继续展示图片/视频提取入口。

## 测试验证

- 已运行 `./gradlew :app:compileDebugKotlin`，结果通过。
- 手动验证点：详情页加载态、错误态、正文滚动、复制、删除选择弹窗、图片提取和视频提取入口行为不变。

## 已知取舍

- 不把详情页底部按钮区抽成共享组件，因为它绑定剪贴记录、链接提取路由、复制和删除业务。
- 不把链接提示抽成公共组件，因为当前只有详情页需要该说明和可点击链接复制语义。

## 变更记录

- 2026-05-18：新增详情页方案文档，并记录详情页普通内容卡片接入 `ClipMasterCard`；原因是本次整理触及详情页 UI 外壳，需要同步本地方案文档说明最终实现和保留的页面业务边界。
