# AGENTS.md

## Execution Quick Reference

- 先查复用：按当前 feature/package、当前模块、共享模块、全仓搜索的顺序确认是否已有相近能力，避免重复实现。
- 再同步文档：涉及方案、架构、行为、UI、数据契约或规则变化时，先或同步更新 `docs/` 下对应方案文档。
- 再实现改动：入口类负责流程编排，工具能力、共享组件和副作用按职责拆到可预测位置，保持小步行为等价。
- 再验证结果：涉及 Kotlin、Compose 或 Android 改动时优先运行 `./gradlew :app:compileDebugKotlin` 或更贴近模块的验证命令。
- 最后收尾检查：确认文档、字符串资源、生命周期、复用/拆分、日志计划与隐私日志、测试/待办和 commit 范围是否符合本文件规则。

## Mandatory Coding Gate

- 本项目后续新增或更新 `.codex/skills/` 下的项目专用 skill 时，必须默认使用简体中文编写；除 skill 目录名、`name`、命令、路径、类型名、配置 key、稳定协议字段和第三方专有名词外，触发说明、正文工作流、UI 元数据、维护规则和示例说明都应使用简体中文。
- 本项目专用 skill 位于 `.codex/skills/`，只适用于当前仓库；后续代码修改、重构、修复、UI 调整、文档/日志/备份相关任务，必须按仓库硬规则 + 项目内 skill 工作流 + 最终回复强制自检执行，不能把这些项目专用 skill 当成其他仓库的通用规则。
- 每次新增或修改代码前，必须先向用户发送一句简短执行说明，说明将检查哪些已有能力、将更新哪份方案文档或为什么不需要更新、预计如何拆分组件/类/方法、将运行什么验证；如果用户明确要求“只回答、不修改代码”，则不进入本门禁。
- 复用检查是进入代码修改前的硬门禁：必须先用 `rg`、`rg --files`、类型引用或等价方式，按当前 feature/package、当前模块、共享模块、全仓兜底的顺序查找相近组件、类、方法、工具、Repository、Mapper、Formatter、Parser、Validator、Dialog/Sheet/Card/Toolbar/EmptyState/LoadingState/ActionRow 等能力。
- 文档检查是进入代码修改前的硬门禁：涉及方案、架构、行为、UI、数据契约、日志、备份恢复、页面生命周期、共享组件、规则或用户可见流程变化时，必须先或同步更新 `docs/` 下对应方案文档；如果判断不需要更新文档，必须在最终说明中写明原因。
- 职责检查是进入代码修改前的硬门禁：新增逻辑不能继续塞进已经过大的页面、Repository、ViewModel、Worker、万能工具类或单个 Compose 文件；如果短期必须局部实现，必须在方案文档“后续待办/开放问题”或最终说明中记录为什么暂不拆分、风险和后续收敛方向。
- Compose 检查是进入 UI 代码修改前的硬门禁：新增 `@Composable` 前必须确认是否已有可复用组件，是否应抽为独立组件，是否需要稳定 UI state/config/sealed state，是否能通过 slot/callback 表达差异，是否避免大量 Boolean、可空参数或隐式组合状态。
- 类和方法检查是进入实现前的硬门禁：新增类、方法、扩展函数、mapper、comparator、parser、formatter、validator 必须有单一职责、可预测命名、清晰输入输出和失败语义；业务 mapper/comparator/parser/formatter 不应随手放到入口类底部，除非已经说明临时原因。
- 日志检查是进入关键逻辑修改前的硬门禁：涉及网络、文件读写、数据库写入、后台任务、权限、系统 API、长任务、重试、缓存、解析、迁移、恢复、备份或错误分支时，必须确认方案文档已有日志与诊断计划，并确保日志点、级别、字段、脱敏和禁止输出内容明确。
- 验证检查是进入收尾前的硬门禁：Kotlin、Compose 或 Android 改动后必须运行对应编译或测试命令；如果因耗时、环境或权限无法运行，必须在最终说明中明确未验证项和原因。
- 如果任一门禁未通过，不允许直接进入代码修改；必须先调整方案、拆分设计、补充文档、说明例外原因或征求用户确认。

## Final Response Rule For Code Changes

- 每次完成代码修改后的最终回复必须简要说明“方案文档”：更新了哪份方案文档；如果没有更新，必须说明为什么不需要更新。
- 每次完成代码修改后的最终回复必须简要说明“复用检查”：复用了哪些已有能力；如果没有复用，必须说明职责不符、依赖方向不允许、契约不稳定、改动风险过高或其他原因。
- 每次完成代码修改后的最终回复必须简要说明“组件/类/方法”：新增、拆分或调整了哪些组件、类、方法、mapper、formatter、validator 或工具；如果存在明显可拆分但暂未拆分的情况，必须标明临时例外和后续收敛方向。
- 每次完成代码修改后的最终回复必须简要说明“规则例外”：是否存在未拆分、未补测试、未新增日志、未更新备份逻辑、未做 release/R8 验证或其他规则例外；没有例外时也要明确说明。
- 每次完成代码修改后的最终回复必须简要说明“验证结果”：列出已运行的验证命令和结果；如果未运行验证，必须说明原因和风险。
- 最终回复应保持简洁，但不能省略以上自检项；如果任务非常小，也可以把这些信息压缩到一小段中说明。

## Plan Documentation Convention

- 后续所有方案设计、需求澄清、实现计划、架构调整、代码修改和行为变更，都必须同步维护本地方案文档，确保文档与最新讨论和代码保持一致。
- 方案文档应优先保存在 `docs/` 目录；如果某个功能已经有对应文档，后续修改必须优先更新原文档，避免创建重复、分叉或互相矛盾的说明。
- 修改代码前如果涉及既有方案变化，必须先或同步更新对应方案文档，记录变更原因、影响范围、数据/接口/UI 调整、边界条件和后续待办。
- 修改代码后如果实现与方案存在偏差，必须同步修正文档，说明最终采用的实现、舍弃的方案以及取舍原因，避免文档停留在过期设计。
- 新增方案文档时，需要写清楚当前状态、目标、范围、用户体验、数据流、涉及文件、实现步骤、测试验证、已知取舍、开放问题和变更记录；后续小改动也要更新“变更记录”或相应章节。
- 新增或更新方案文档时，必须包含“日志与诊断计划”或等价章节；需要明确哪些关键流程、状态变化、异常分支、重试/降级、后台任务、网络/文件/数据库副作用和用户关键操作需要打印日志，并写清每个日志点的级别、触发时机、字段内容、脱敏策略、禁止输出内容和验证方式。
- 如果某次计划判断不需要新增日志，必须在方案文档或最终说明中写明原因，例如纯 UI 文案调整、无副作用的样式调整、已有日志已覆盖或日志会带来隐私/噪声风险，避免默认遗漏可观测性设计。
- 每个方案文档顶部必须标注当前状态，状态限定为 `草案`、`已确认`、`实现中`、`已完成`、`已废弃` 中的一种；当需求确认、开始实现、完成实现或废弃方案时，必须同步更新状态。
- 每次修改方案文档时，必须在“变更记录”中追加日期、改动内容和改动原因，方便后续追踪需求变化和实现取舍。
- 当某个组件、公共数据层、通用交互或跨页面能力被多个页面复用时，应只为该共享能力创建一份主方案文档；主文档负责承载完整设计细节、状态流转、交互规则、数据契约、边界条件和测试验证，避免按使用页面重复创建多份组件设计文档。
- 使用共享组件或共享能力的页面文档，只需要引用对应主方案文档，并补充本页面的接入方式、影响摘要、差异化行为、入口/出口约束和例外情况；不要在页面文档中重复维护共享组件的完整结构、样式、交互和测试细节。
- 共享能力发生变更时，优先更新主方案文档；只有当变更改变某个页面自身的用户体验、生命周期、导航关系、权限边界、数据范围或与共享能力的差异化约定时，才同步更新对应页面文档。页面文档的更新应保持简短，说明“本页面如何受影响”和“最终以哪个共享文档为准”，避免公共组件的小改动造成多份文档大面积重复修改。
- 如果发现多个方案文档已经重复描述同一共享实现，应在后续维护中逐步收敛：保留主文档里的完整描述，其他文档改为摘要和引用式说明；如果页面存在特殊分支或例外，必须在页面文档中明确写出差异和原因。
- 如果发现方案文档与当前代码行为或最新需求冲突，必须立即修正文档或代码，并在文档中说明最终以哪个行为为准，不能让冲突长期保留。
- 如果代码实现偏离原方案，必须在对应文档中补充最终实现、未采用方案和取舍原因，确保文档描述的是当前真实设计。
- 每次完成任务前必须把“是否需要更新方案文档”作为收尾检查项；如果相关需求、方案或代码发生变化，就必须同步更新对应文档。
- 尚未确定的需求点、交互细节、性能取舍或后续增强，必须集中记录在方案文档的“开放问题”或“后续待办”章节，避免散落在正文中造成误解。
- 当前剪贴结果列表共享组件主文档位于 `docs/clip_result_list_plan.md`，后续 `ClipResultList`、`ClipCard`、共享剪贴 item、右滑菜单、第二段滑动动作、列表结果复用、关键词高亮、item 高度测量、离场动画或取消置顶滚动修正相关改动，都必须优先更新该文档；列表页、搜索页和折叠页文档只记录本页面接入方式、影响摘要和差异化行为。
- 当前搜索页方案文档位于 `docs/search_page_plan.md`，后续搜索入口、搜索筛选、搜索结果高亮、列表结果复用或相关数据层改动，都必须同步更新该文档。

## Page Lifecycle and Performance Convention

- 编写页面时必须优先从页面生命周期出发设计数据收集、任务启动、媒体读取、分页加载、传感器/定位、WebView、后台轮询和其他重型操作，确保页面进入可见/前台状态时才开始，页面离开可见/前台状态时能及时暂停或取消，避免不可见页面继续消耗 CPU、IO、内存、网络或电量。
- Compose 页面收集 Flow、Paging、数据库观察、权限状态、服务连接和一次性事件时，应优先使用 `collectAsStateWithLifecycle`、`flowWithLifecycle`、`repeatOnLifecycle` 或等价生命周期感知方案；不要在 ViewModel 初始化阶段默认启动会读取大量数据、扫描本地媒体、执行网络请求或持续观察数据库的任务，除非该任务确实与页面可见性无关并已说明原因。
- Tab、筛选条件、Pager 页码、SegmentedButton 等页面内控件只能表达“当前用户选择查看什么内容”，不能替代页面生命周期来决定“页面是否应该工作”；切换 Tab 可以决定收集哪一路数据，但数据流的启动、暂停和取消仍必须受页面生命周期约束。
- 多 Tab、多页面或横向 Pager 场景中，应避免因为默认选中项而同时加载所有分类数据。默认只加载当前可见页面或当前 Tab 必需的数据；切换到其他 Tab 后再按需加载对应数据，并在离开当前 Tab 或页面不可见时停止不必要的观察和重型处理。
- 面对可能增长的数据集合，优先使用分页、分批、懒加载或按需查询；标题栏数量、全选、清空、运行中状态等全局信息应使用轻量 `COUNT`、id 查询或专用聚合接口，不应依赖已经加载的可见列表，也不应为了统计而一次性加载全部实体。
- 涉及页面生命周期、分页加载、按需加载、缓存策略或重型任务启停的实现，必须在对应方案文档和代码注释中说明触发时机、取消边界、性能取舍以及为什么放在页面层或 ViewModel 层，避免后续维护时把生命周期约束误改成控件状态驱动。

## Code Organization and Reuse Convention

- 后续新增或修改代码前，必须先检查项目里是否已经存在相同或相近能力，优先使用 `rg`、`rg --files`、IDE 搜索或类型引用查找已有方法、扩展函数、工具类、Repository、Helper、Parser、Formatter、Validator 和平台封装，避免重复实现。
- 如果发现已有能力能满足需求，应优先复用；如果只差少量参数或边界，应优先在原有抽象上扩展，并同步评估所有调用方影响。只有当现有能力职责不符、依赖方向不允许、语义会被污染或复用会带来明显风险时，才新增实现，并在代码注释或方案文档中说明不复用的原因。
- 复用和抽象必须服务于清晰边界，不能为了减少行数或追求“统一”而过度抽象。只有结构、状态、交互、输入输出契约或失败语义确实相近的代码才合并；业务语义不同但表面长得像的逻辑，应保持独立或只复用更底层的纯能力。
- 禁止把网络请求、文件读写、格式识别、内容校验、MediaStore 发布、日志诊断、UI 状态编排、业务流程调度等多类职责长期堆在同一个大类或大文件中。Worker、ViewModel、Composable、Repository 等入口类应主要负责流程编排和状态连接，具体能力应按职责下沉到明确命名的协作者、工具类或扩展方法。
- 当一个文件持续增长、私有方法跨越多个职责域、方法命名开始依赖注释才能理解分组，或某类逻辑已经可能被其他页面/Worker/模块复用时，应优先规划拆分。拆分时按业务语义命名，例如 `ImageFormatSniffer`、`ImageDownloadValidator`、`ImageRequestHeaderBuilder`、`MediaStoreImagePublisher`，避免创建含糊的 `CommonUtils`、`HelperUtils` 或万能工具类。
- 纯函数、字节解析、字符串规范化、轻量格式判断等无状态能力，优先做成扩展函数或小型 `object`；需要持有 `Context`、Repository、OkHttp、系统服务、协程调度器或可替换依赖的能力，优先做成职责单一的类并通过构造函数注入依赖，避免隐藏全局状态。
- 工具类、扩展方法和共享组件默认优先使用模块内可见性（如 Kotlin `internal`），减少不必要的公开 API 面；只有确认跨模块复用、依赖方向合理且契约稳定时，才放入共享模块并公开。
- 工具能力必须写清楚输入输出和失败契约：解析失败是返回 `null`、抛异常、返回 `Result`/sealed result，还是降级到兜底值，必须在方法注释和类型设计中保持一致；同一个工具类内不要混用多种失败语义，除非方法名和文档明确区分。
- 纯逻辑工具应尽量不依赖 Android `Context`、View、系统服务或全局状态，尤其是字节解析、URL 规范化、文件名清理、候选去重、格式识别和内容判断，应优先设计成可单元测试的纯 Kotlin 能力。
- 工具能力的放置位置必须遵守模块边界：仅当前功能使用的能力放在对应 feature/package 下；跨页面或跨 Worker 复用的能力应放到合适的共享模块或已有通用包中；禁止为了复用让底层模块反向依赖上层业务模块。
- 工具类和扩展方法必须放在后续维护者按“领域 + 职责”最容易想到的位置，不能为了省事随手塞进当前大文件、顶层 `utils` 或已经过载的工具类。新建位置时应优先使用清晰的领域包和职责文件名，例如图片格式识别放到 `image/format` 相关包，图片下载校验放到 `image/download` 相关包，字节头解析扩展放到接近格式解析职责的位置。
- 查找已有能力时应遵循由近到远的顺序：先查当前 feature/package 内同职责文件，再查当前模块已有的 helper、parser、validator、formatter、publisher、repository 或 platform wrapper，然后查共享模块对应领域目录，最后才做全仓搜索。只有当局部约定不清晰、命名不可预测或跨模块关系不确定时，才使用全仓 `rg` 兜底。
- 工具类、扩展文件和包名必须可预测，尽量让调用方不打开文件也能判断职责；优先使用 `ImageFormatSniffer`、`ImageAnimationMetadataReader`、`ImageDownloadValidator`、`ImageRequestHeaderBuilder`、`MediaStoreImagePublisher`、`ByteArrayImageHeaderExtensions.kt` 这类具名职责，避免 `FileUtils`、`CommonUtils`、`ImageUtils` 继续膨胀成万能入口。
- 扩展方法应按 receiver 类型和业务领域共同归类：同一 receiver 的通用能力可以集中，但带明显业务语义的扩展必须靠近对应领域；例如 `ByteArray` 的图片头解析不应散落在 Worker 中，也不应混进与图片无关的通用字节工具，避免后续查找只能依赖全仓扫描。
- 扩展方法要谨慎挂到 `String`、`File`、`ByteArray`、`Context` 等常见类型上，避免污染调用提示和制造语义冲突。带业务语义的扩展必须使用具体命名并靠近领域包；如果方法需要外部依赖、隐藏副作用或复杂失败处理，应优先使用具名类方法而不是扩展方法。
- 某个领域工具数量增长时，应继续按子领域建立索引感强的包或文件，例如 `format`、`download`、`preview`、`selection`、`publisher`；不要让新增能力散落到多个无规律目录，也不要回流到万能工具类。
- 大文件拆分应按低风险到高风险的顺序推进：优先拆纯函数、解析、格式化、映射和校验规则；其次拆 IO、网络、系统 API、MediaStore、权限等平台封装；再拆可复用 Compose 组件和 UI state；最后才拆主流程编排、状态机和跨模块契约，避免重构一开始就触碰最容易产生行为回归的核心流程。
- 工具类和组件命名后缀应表达语义：`*Sniffer` 只识别不修改，`*Reader` 读取元数据且不产生业务副作用，`*Validator` 负责校验并有明确失败契约，`*Builder` 构造对象或请求，`*Publisher` 写入外部系统或发布媒体，`*Formatter` 生成展示文本，`*Mapper` 做数据结构转换，`*Parser` 解析外部输入，`*Repository` 管理数据来源，`*Helper` 仅用于很小且难以归入更具体语义的协调逻辑。
- 实体类、模型类和 data class 默认按领域、层级和使用范围放置，不要因为它们都是“实体”就集中塞进一个全局 `entity` 目录；Room 实体、跨页面契约、Worker 内部结果对象、UI state、序列化模型应分别放在后续维护者最容易按职责找到的位置。
- 需要反混淆的实体类或模型类必须使用统一注解标记，例如 AndroidX `@Keep` 或项目自定义的 `@KeepForSerialization` / `@KeepForContract`，并在类型注释中说明需要稳定类名、字段名或构造函数的原因；混淆规则应优先按注解统一匹配，避免在 ProGuard/R8 文件里逐个追加类名。
- 反混淆范围按外部契约和运行时机制决定，而不是按 `data class` 身份决定。只有被 JSON/序列化、Intent/通知协议、WebView JS 协议、第三方 SDK、反射、系统框架或跨模块稳定 ABI 依赖的模型才应 keep；Worker 内部边界对象、页面 UI state、纯流程结果和只在编译期调用的领域模型默认不反混淆。
- 如果某一组模型本身就是稳定外部协议，可以放到可预测的 `contract`、`serialization`、`protocol` 或同等领域目录；但目录归口不能替代注解标记，新增或移动这类模型时必须同步检查混淆规则、序列化字段名和调用方契约。
- JSON、序列化、WebView JS、Intent extra、通知协议或第三方接口模型必须优先用字段协议注解显式固定外部字段名，例如 `@SerializedName`、`@SerialName` 或项目等价注解；不能默认依赖 Kotlin 属性名、枚举 `name` 或类名在混淆后仍然可读。
- 配置反混淆前必须先判断 keep 粒度：是需要保留类名、字段名、方法/构造函数、枚举值，还是只需要保留某个注解成员。禁止默认使用过宽的 `-keep class ** { *; }`，除非已经说明为什么细粒度规则无法满足运行时契约。
- 禁止为了省事把 `entity`、`model`、`data`、`dto` 等宽泛包整包关闭混淆；包级 keep 只允许用于明确承载稳定外部协议的 `contract`、`serialization`、`protocol` 等目录，并且仍应配合注解和注释说明稳定边界。
- 如果项目新增自定义 keep 注解，必须按语义拆分并写清用途，例如 `@KeepForSerialization` 表示序列化框架依赖字段/构造，`@KeepForContract` 表示 Intent/通知/JS/SDK/跨模块协议依赖，`@KeepForReflection` 表示运行时反射依赖；不要用含糊的万能注解掩盖不同风险。
- 新增反混淆标记时，类型或字段注释必须说明“谁依赖它稳定”和“稳定的是类名、字段名、构造函数、方法签名、枚举值还是序列化 type name”；后续移除、重命名或迁移时必须同步复核这些依赖。
- 枚举和 sealed class 参与外部协议、数据库、JSON、多态序列化、Intent 或 JS 传递时，必须显式定义稳定 code/type 字段或序列化 type name，禁止直接把枚举 `name`、ordinal 或 Kotlin sealed 子类名当作长期协议。
- 涉及混淆规则、反射模型、序列化模型、通知/Intent 协议、WebView JS 协议或第三方 SDK 回调模型的改动，应优先补充 release/R8 相关验证；如果本地无法运行混淆构建，最终说明和方案文档必须明确未验证项与风险。
- 库模块对外暴露需要稳定的模型、反射入口或序列化契约时，混淆规则应优先放在该模块的 consumer rules 或等价位置，跟随模块分发；不要只依赖 app 模块集中兜底，避免复用到其他宿主时规则丢失。
- Room 实体默认不因为“是数据库实体”就反混淆；只有当实体同时被外部协议、反射、序列化、导出 schema 读取或跨模块 ABI 稳定性依赖时才标记 keep。Room 字段改名、列名和迁移应优先依赖 `@ColumnInfo`、迁移脚本和编译期校验，而不是粗暴 keep 整个实体包。
- Release 混淆构建必须保留可回溯的 mapping 文件，并确保线上崩溃、日志和问题定位能拿到对应版本 mapping；日志中仍不得输出 Token、Cookie、完整用户输入、可恢复登录态或其他敏感字段，即使这些字段因 mapping 可还原也不能泄露。
- Compose 组件同样必须遵守职责拆分和复用规则。新增 `@Composable` 前必须先查当前页面、当前 feature、`ui/widget`、共享组件文档和已有设计系统组件，确认是否已有同类 Dialog、BottomSheet、Card、Toolbar、Grid、Preview、EmptyState、LoadingState、ActionRow 或选择控件可以复用或扩展。
- 页面级 Composable 应主要负责状态收集、事件连接和页面结构编排；可复用的弹窗、预览、工具栏、列表项、网格项、状态块和操作区应抽成独立 Composable，并按 feature-local 或 shared-ui 职责放到可预测文件中，避免长期堆在单个页面文件里。
- Compose 组件默认应按“可复用公共组件”设计和放置。除非组件与当前页面存在非常强的业务耦合，只能服务该页面，否则都应优先抽为 feature 级或 `ui/widget` 等共享组件，方便其他页面和后续需求直接复用。
- Compose 组件公共化应按复用半径分层：只服务同一 feature 多页面的放在 feature 的 `component`、`widget` 或同等目录；跨 feature、跨业务域或后续页面大概率会复用的放到 `ui/widget`、共享 UI 模块或既有设计系统；不要因为“公共化”就把所有组件无差别塞进同一个目录。
- 页面私有 Compose 组件只适用于强绑定当前页面状态机、页面专属 ViewModel、导航目的地、权限流程、一次性副作用、路由参数或特定业务实体且难以通过 UI state、slot 和 callback 解耦的场景；如果组件只是视觉结构、交互模式、布局容器或状态展示相似，就不能因为当前只有一个页面在用而默认留在页面目录。
- 页面私有 Compose 组件必须能说清“为什么只能服务当前页面”。如果理由只是当前暂时只有一个页面使用、调用方文案不同、按钮略有不同、数据实体不同或入口位置不同，应优先通过 UI state、slot、callback、配置对象或薄适配层公共化。
- 新增或拆分 Compose 组件时，应先尝试把页面实体转换为稳定 UI state，再让共享组件接收 state、slot 和事件回调。页面 package 可以保留薄适配层，但通用 UI 骨架、交互控件、预览弹层、选择网格、状态块、操作栏、筛选控件和常见 Dialog/Sheet 默认应放入共享位置。
- 如果因为改动风险、需求不稳定或依赖边界暂时无法把明显可复用的 Compose 组件公共化，必须在对应方案文档的“后续待办”或“开放问题”中记录暂不公共化的原因、影响范围和后续收敛方向，避免页面私有组件长期沉淀成重复实现。
- 当两个 Compose 组件视觉结构、交互流程或数据契约高度相似时，应优先收敛为一个带参数、slot 或状态提升接口的共享组件，而不是复制一份再微调。像图片预览 Dialog、图片预览 BottomSheet、媒体预览卡片这类组件，如果只是入口、按钮或元信息略有不同，应设计统一组件承载差异。
- Compose 共享组件必须保持稳定 API：参数命名、默认值、slot 语义、回调时机和状态模型一旦被多个调用方使用，修改前必须评估所有调用方和方案文档；不能只为了当前页面临时需求破坏共享组件契约。
- Compose 组件也要避免过度抽象。只是“都像卡片/弹窗/列表项”但业务语义、状态流转或交互边界不同的组件，不要硬合并成万能组件；可以抽取更底层的排版片段、样式 token 或纯展示子组件。
- Compose 组件放置位置必须便于查找：仅当前页面使用的私有小组件可以留在页面文件附近；当前 feature 多页面复用的组件应放到 feature 的 `component`、`widget` 或同等职责包；跨 feature 复用的组件应放到 `ui/widget`、共享 UI 模块或既有设计系统位置，并在页面方案文档中只记录接入差异。
- 抽取 Compose 组件时必须保持状态边界清晰：业务状态尽量由调用方传入并通过事件回调向外抛出，组件内部只保留 UI 局部状态；涉及 `remember`、`LaunchedEffect`、`DisposableEffect`、滚动状态、弹窗显示状态或资源加载状态时，必须说明状态保存边界和生命周期影响。
- 共享 Compose 组件禁止直接依赖具体页面的 `ViewModel`、`NavController`、Repository 或业务单例；组件应接收稳定的 UI state/data 和事件 callback，由调用方负责状态收集、导航和业务动作，确保组件可以在其他页面、Preview 或测试场景中独立复用。
- Compose 组件参数应表达清晰的 UI 契约。复杂状态优先定义具名 UI state data class、config data class、sealed state 或明确的参数组，禁止用大量零散 Boolean、可空参数和隐式组合驱动分支；如果参数开始出现 `isXxx`、`showXxx`、`enableXxx` 堆叠，必须优先评估是否应改为状态模型、配置对象或 slot，避免公共组件变成难以维护的万能组件。
- 共享组件差异优先用 slot、内容 lambda、配置对象或状态提升表达，例如 `titleContent`、`actions`、`footer`、`emptyContent`；所有公共 Composable 默认提供 `modifier: Modifier = Modifier`，复杂区域优先用 slot 扩展，避免为了单个页面新增一次性参数。只有当视觉结构、交互语义或生命周期边界确实不同，才拆成不同组件，避免复制出多个只差按钮或标题的 Dialog/Sheet。
- 共享组件的文案策略必须明确：真正跨页面通用且语义稳定的文案可以在组件内通过 `stringResource` 读取；带页面语义、业务动作、错误原因、统计口径或流程提示的文案应由调用方传入，避免共享组件悄悄绑定某个页面语境。
- 共享 Compose 组件的颜色、字号、圆角、间距、图标和交互反馈应优先来自 `MaterialTheme`、项目已有设计系统、资源或统一 token；除非组件有明确领域语义，不要在组件内部私自写一套不可复用的样式常量。
- 加载中、失败重试、空态、权限提示、图片预览、选择网格、批量操作栏等高频 UI 状态应优先沉淀为统一组件；页面只负责决定当前展示哪个状态和传入对应操作，避免每个页面重复绘制类似状态块。
- 共享 Compose 组件必须考虑边界内容和状态：长标题、空数据、加载中、失败、无权限、图片加载失败、超小屏幕、横竖屏、深色模式和动态字体都应在设计或 Preview 中覆盖，不能只按当前页面的理想数据编写。
- LazyColumn、LazyVerticalGrid、Pager 等可滚动集合中的 item 必须优先提供稳定 key；涉及选择状态、展开状态、动画、下载状态或图片候选增删时，禁止依赖列表下标作为长期身份，避免刷新后状态错乱、滚动跳动或复用错误。
- Compose 组件命名必须同时体现用途和形态，例如 `ImagePreviewDialog`、`ImagePreviewSheet`、`SelectableImageGrid`、`ImageCandidateTile`；避免 `MyDialog`、`CommonDialog`、`ItemView`、`ContentView` 这类无法从名称判断职责的命名。
- 单个 Compose 组件文件可以包含主组件及其紧密私有小片段，但不能继续膨胀成多个无关组件的大杂烩；如果文件里出现多个可独立复用的 Dialog、Sheet、Card、Tile 或状态组件，应继续按职责拆分到独立文件。
- 共享组件内部只应管理展开、滚动、动画、焦点、临时输入等纯 UI 局部状态；网络请求、数据库观察、权限申请、导航、下载启动、跨页面事件、一次性业务副作用和业务协程默认放在调用方、ViewModel 或专门 state holder 中。若公共组件确实需要资源加载监听、播放器、传感器或其它副作用，必须把触发 key、启动条件、取消边界和适用场景写清楚。
- Compose 组件内部如确实需要 `LaunchedEffect`、`DisposableEffect`、动画、播放器、传感器、图片加载监听或其它副作用，必须让触发 key、启动条件和取消边界清晰可见，并在注释中说明为什么副作用放在组件内部而不是调用方。
- 复杂共享组件必须优先提供 `@Preview` 或可独立构造的 sample state，尤其是 Dialog、Sheet、Grid、Card、预览器、筛选控件和状态组件；预览至少覆盖正常态、空态/失败态、长文案或极端内容中的关键场景，方便后续独立检查布局、主题和长文本适配，而不必启动完整业务页面。
- 当 Compose 组件被多个页面复用时，应优先建立或更新对应共享组件主方案文档，完整记录组件状态流转、参数契约、slot 语义、交互边界和测试验证；页面文档只记录本页面接入差异，避免多处重复维护同一组件设计。
- 拆分既有大文件时应保持行为等价，优先小步迁移：先移动同一职责的一组方法并保留原有测试/验证路径，再逐步收敛调用方。除非用户明确要求重构行为，否则不要在职责拆分中夹带功能改动、排序规则变化、过滤条件变化或异常处理变化。
- 新增工具类、扩展方法或共享能力后，必须按本文件的注释规则补充简体中文注释，说明职责、适用范围、边界条件和不适用场景；如果该能力影响既有方案或多个页面，也必须同步更新对应方案文档。
- 代码评审或收尾时如果发现某个文件已经承担过多职责，应主动指出可拆分方向；如果本次任务无法立即拆分，应把拆分建议记录到对应方案文档的“后续待办”或“开放问题”，避免问题继续隐性累积。
- 每次新增或修改代码前，应做轻量复用检查：当前 feature/package、当前模块、共享模块是否已有相近能力；如果最终没有复用，应在注释、方案文档或最终说明中记录原因，例如职责不符、依赖方向不允许、契约不稳定或复用风险过高。
- 每次完成任务前必须做规则符合性收尾检查：是否需要更新方案文档、是否有用户可见硬编码字符串、是否新增过大文件/过大函数/重复组件/重复工具、是否违反页面生命周期约束、是否有不稳定 Lazy key、是否存在隐私日志风险、是否需要补充测试或后续待办。
- 允许有例外，但例外必须可追踪。紧急修复、线上风险、改动面过大、拆分风险高或需求仍不稳定时，可以先采用局部实现；但必须在方案文档“后续待办/开放问题”或最终说明中记录为什么暂不拆分、暂不复用或暂不补测试，并说明后续收敛方向。
- 文件和函数规模使用软阈值触发评估而非机械强制：Kotlin 文件超过约 500 行、Compose 页面/组件文件超过约 400 行、单个函数超过约 80 行、单个 Composable 参数过多或状态分支过深时，必须主动评估是否需要拆分职责、抽 UI state、抽组件或抽工具；若暂不拆分，需要说明原因。
- 规则发生冲突时按优先级取舍：用户明确要求和数据安全优先，其次是编译通过、行为兼容、文档同步、可测试性、复用和抽象优雅。不能为了满足抽象或文件长度规则而牺牲用户数据、现有行为或交付可靠性。
- 新增共享组件、工具类或扩展方法时，应在注释或方案文档中写明适用场景和不适用场景，避免后续被滥用成万能入口。
- 公共纯逻辑能力应逐步补充单元测试，优先覆盖字节解析、URL 规范化、文件名清理、候选去重、格式识别、排序过滤和失败契约；不要求每次重构一次性补齐所有测试，但新增复杂纯逻辑时应优先考虑可测性。
- 提交时应说明改动范围与规则关系：如果同一提交同时包含规则文档和代码改动，应在 commit 正文中说明规则变更是否驱动了本次代码调整；如果只是规则沉淀或纯代码重构，优先保持提交目的单一，避免把无关功能变化混入。

## Logging Convention

- 日志目标是帮助定位流程、状态、失败原因和性能边界，不是记录业务数据本身。新增或修改涉及网络、文件读写、数据库写入、后台任务、跨进程、权限、系统 API、长任务、重试、缓存、解析、迁移、恢复、支付或账号等关键逻辑时，必须同步评估是否需要日志。
- 日志正文、阶段描述、错误说明和人工可读的提示内容必须统一使用简体中文，避免中英文混杂；仅稳定字段名、TAG、枚举值、reasonCode、traceId/taskId/requestId 等结构化标识可以保留英文或既有命名。
- 日志设计必须在方案文档中提前说明；实现时日志点要与方案一致，若最终增删日志点，必须同步更新方案文档的“日志与诊断计划”或变更记录。
- 日志必须能串起一次关键流程。异步任务、Worker、长任务、批处理、网络请求链路和跨层流程应生成或传递短 `traceId`/`taskId`/`requestId`，同一次流程的开始、关键阶段、成功、失败和清理日志都应带上该标识。
- 日志级别要稳定：`logD` 用于开发期流程节点、参数摘要、跳过原因和数量统计；`logI` 用于用户主动触发或后台关键任务的成功结果；`logW` 用于可恢复异常、降级、重试、部分成功、数据不完整但可继续的情况；`logE` 用于任务失败、数据校验失败、不可自动恢复错误、用户操作被阻止和需要排查的异常。
- 每个关键流程原则上至少记录开始、成功和失败。复杂流程还应记录阶段性边界，例如参数规范化完成、权限校验完成、缓存命中/未命中、文件写入完成、事务提交完成、远端发布完成、重试已排队和资源清理完成。
- 失败日志必须包含可行动的失败分类或 reasonCode，例如 `auth_failed`、`permission_denied`、`storage_not_writable`、`network_timeout`、`parse_failed`、`checksum_mismatch`、`schema_unsupported`、`cancelled`、`rate_limited`；不要只输出“失败”或只依赖异常 message。
- 日志字段优先使用稳定、低敏、结构化的键值片段，例如 `taskId=... operation=... status=... reasonCode=... count=... durationMs=... retryable=...`，避免拼接大段自然语言导致搜索困难。
- 日志中可以记录数量、布尔值、枚举、状态码、耗时、文件大小、分页页码、批次大小、脱敏短标识、哈希前缀和是否存在；禁止记录业务实体完整内容、数据库整行 dump、请求/响应全文、完整 JSON、完整 HTML、完整剪贴内容或用户输入全文。
- 严禁在日志中输出 Cookie、Token、Authorization、密码、验证码、密钥、完整个人敏感数据、可恢复登录态、WebDAV 密码、本地授权 URI 原文、完整下载 URL、完整 URL 查询参数、剪贴内容、搜索词全文、图片 OCR 文本全文和备份包内容。
- URL 日志只能保留 scheme、host、必要路径类型或脱敏后的 path 片段；查询参数默认全部去除。确需定位参数存在性时，只记录参数名白名单、参数数量或参数值哈希，不能记录原值。
- 用户输入、剪贴内容、搜索词、文件内容、网页标题、OCR 文本等高敏内容如确需排查，只能记录长度、是否为空、类型、摘要哈希或极短脱敏片段；默认不记录片段，除非方案文档明确说明原因和风险。
- 本地文件路径、SAF URI、MediaStore URI 和远端路径默认只记录文件名、扩展名、目录显示名、是否存在、大小和授权状态；不得记录可直接访问的完整 URI 或包含用户目录结构的完整绝对路径。
- 网络日志不得记录请求体、响应体、完整 header 或第三方服务返回的原文错误；如需定位，记录状态码、错误分类、响应大小、耗时、重试次数和脱敏 host。
- 异常日志可以传入 throwable，但日志正文不能直接拼接可能含敏感数据的 `throwable.message`。需要展示错误细节时，优先记录异常类型、reasonCode 和脱敏后的上下文。
- 循环、分页、下载进度、输入监听、滚动、动画和高频回调中的日志必须限频、抽样或只记录状态变化，避免刷屏、性能抖动和日志文件膨胀。
- 后台任务和定时任务日志必须记录调度原因、约束条件、是否跳过、是否重试、下次重试/延迟策略和最终状态；跳过不是失败，必须有明确 skip reason。
- 涉及数据迁移、恢复、批量删除、清理、覆盖、同步、导入导出等高风险操作时，日志必须记录操作范围摘要、输入校验结果、事务开始/提交/回滚、影响数量和失败分类，但不能记录被处理的数据内容。
- 涉及隐私、安全、权限和账号状态的日志必须默认更保守；如果“排障价值”和“泄露风险”冲突，优先保护用户数据，并在方案文档记录无法打印详细日志的原因。
- Release 构建中的日志同样必须遵守脱敏规则；不能因为线上问题难排查就输出敏感数据。若某些 debug-only 日志只允许开发包出现，必须通过构建类型、日志封装或显式开关隔离。
- 新增日志前应优先复用项目已有日志封装、TAG 命名和脱敏工具；如果缺少脱敏工具，优先补充职责明确的小工具，而不是在各处手写不一致的字符串处理。
- 日志 TAG 应表达模块或职责，避免使用个人名、临时名或过宽的 `Utils`。同一类流程的 TAG 和字段名保持一致，方便搜索和聚合。
- 提交前收尾检查必须包含日志检查：计划是否写明日志点、实现是否覆盖计划、日志级别是否合适、是否存在敏感信息泄露、是否有高频噪声、失败原因是否可行动、是否需要补充测试或手动验证日志输出。

## Backup Coverage Convention

- 当前 WebDAV + 本地备份恢复主方案文档位于 `docs/webdav_backup_plan.md`，后续备份格式、WebDAV、本地备份、恢复预检、恢复报告、自动备份、dirty 标记、备份保留、系统 Auto Backup 排除、备份安全边界或恢复合并策略相关改动，都必须优先更新该文档。
- 后续新增功能、Room 表、DAO 字段、设置项、用户生成数据、下载/缓存元数据、跨安装有意义的 UI 状态或外部协议模型时，必须同步评估是否纳入备份恢复范围，不能只完成业务功能而遗漏备份链路。
- 如果新增数据需要备份，必须同步更新 `BackupSnapshot`/manifest、导出 mapper、恢复 mapper、恢复预检摘要、恢复报告、去重/冲突规则、checksum 覆盖范围、测试用例和 `docs/webdav_backup_plan.md`，确保卸载重装后该数据能按预期恢复。
- 如果新增数据明确不备份，必须在对应方案文档或备份主方案文档说明不备份原因、用户影响和后续可选方向，例如属于临时缓存、设备绑定授权、可恢复登录态、运行中任务状态、媒体文件本体或可重新生成的派生数据。
- 备份导出必须默认使用字段白名单；新增字段不能因为存在于 Room 实体或设置对象中就自动进入备份，尤其是 Cookie、Token、WebDAV 密码、本地目录授权 URI、临时路径、pending URI、健康状态、失败次数和完整用户敏感输入。
- 恢复逻辑必须保持幂等；新增数据纳入备份时需要定义稳定去重 key、冲突合并规则、旧备份兼容行为和失败语义，避免同一备份重复恢复产生重复数据或回滚用户新状态。
- 新增可备份数据时必须检查系统 Auto Backup 规则，确认敏感配置、设备绑定授权和无法跨安装复用的密文不会被 Android 系统备份带走。
- 完成任何新增功能或数据层改动前，收尾检查必须包含“是否需要更新备份逻辑和备份方案文档”；如果暂不接入备份，最终说明或方案文档必须记录原因和后续收敛方向。

## Code Commenting Convention

- 后续新增或修改代码时，必须同步补充简体中文注释，注释重点说明“做什么”“为什么这样做”“有什么边界/代价”，而不是复述语法。
- 新增或修改的类、接口、枚举、密封类、数据类、Worker、ViewModel、Repository、DAO、工具类等类型，都需要添加尽量详细的简体中文注释，说明职责、使用场景、生命周期、线程/协程约束、跨模块契约以及必要的设计意图。
- 新增或修改的方法、构造函数、扩展函数、Composable、回调、协程任务、Worker 入口和工具函数，都需要添加简体中文注释，说明输入输出、核心流程、调用时机、异常/失败处理以及为什么放在当前层级实现。
- 方法注释要求不区分可见性，`public`、`internal`、`protected`、`private` 方法和本文件内的辅助函数都必须按同一标准补充简体中文注释，不能因为只在局部使用就省略说明。
- Compose 相关注释要求覆盖所有 `@Composable` 函数，包括页面入口、私有 UI 片段、布局容器、状态分支、Preview、remember 辅助函数和格式化展示函数；需要说明它承载的 UI 职责、状态输入、用户交互、重组/状态保存边界以及为什么拆成当前函数。
- 新增或修改的变量、常量、配置项、阈值、状态字段、缓存字段、锁、集合、正则、JS 脚本、SQL 片段、Intent extra key、WorkManager key、Bundle key、Preference key、数据库字段名等，都需要添加简体中文注释，说明用途、单位、取值原因、业务含义、有效范围和修改风险。
- 每个实体类的字段都需要添加简体中文注释，说明字段的作用、业务含义、取值限制、是否可空、默认值含义、持久化/序列化约束以及修改时可能影响的读写方。
- 涉及关键分支、提前返回、兜底逻辑、边界条件、重试、超时、去重、排序、过滤、限流、并发控制、线程切换、资源释放、权限判断、版本兼容、存储路径、媒体库行为、网络请求头、Cookie/UserAgent/Referer、反盗链处理、数据迁移、数据库事务、幂等性和非直观实现时，必须补充简体中文注释说明原因。
- 涉及 UI 状态、页面生命周期、Compose 状态保存、副作用、DisposableEffect/LaunchedEffect、WebView 生命周期、JS 注入、DOM 扫描、用户可见文案、通知、下载进度和失败展示时，需要注释说明状态流转和用户体验上的考虑。
- 涉及安全、隐私、权限、文件读写、外部存储、后台任务、前台服务、跨进程、Shizuku、系统 API、反射、隐藏 API 或平台版本差异时，需要注释说明风险、兼容策略和为什么需要这样处理。
- 修改既有逻辑时，如果改动改变了原有行为、默认值、排序规则、过滤规则、保存位置、错误处理或兼容范围，需要在相关位置补充注释说明行为变化和原因。
- 对复杂算法、解析规则、正则表达式、启发式判断、魔法数字、性能优化、内存优化和采样策略，需要注释说明思路、依据、适用范围和可能的误判/取舍。
- 对跨文件或跨模块调用形成的隐含约定，需要在数据结构、常量或调用入口处补充注释，例如某字段由谁写入、谁消费、是否持久化、是否要求顺序稳定、是否可以为空。
- 注释应尽量贴近代码放置：类型注释放在声明上方，方法注释放在方法上方，关键逻辑注释放在分支或代码块前，变量/常量注释放在声明上方。
- 注释要准确维护。修改代码时必须同步更新过期注释；如果发现注释与实现不一致，应优先修正注释或实现，避免留下误导信息。
- 避免无意义的逐行注释，不要解释一眼能看懂的赋值、调用或语法；但当变量名不足以表达业务原因、取值依据、边界条件或副作用时，仍然需要补充注释。

## String Resource Convention

- 所有用户可见字符串必须写入对应模块的 `strings.xml`，代码中通过 `stringResource(...)`、`context.getString(...)` 或 `applicationContext.getString(...)` 引用，禁止在 Kotlin/Java 代码中硬编码用户可见文案。
- 带变量的用户可见文案必须使用字符串资源占位符（如 `%1$d`、`%1$s`），不要用字符串拼接或模板字符串直接拼出中文文案。
- 通知标题/内容、Toast、Dialog、按钮、页面标题、错误提示、进度文本、Intent chooser 标题等都属于用户可见字符串，必须资源化。
- 日志、异常调试信息、数据库状态值、网络参数、文件名规则和内部 key 不属于用户可见字符串，可以保留在代码中；如果它们会展示给用户，则必须改为字符串资源。
- 新增字符串时优先放到使用方所在模块的 `strings.xml`；跨模块复用或 base 模块代码需要使用的文案放到 `base/general/src/main/res/values/strings.xml`。

## Git Convention

- 后续创建 Git commit 时，提交信息必须使用简体中文，简洁概括本次改动的真实范围和目的；commit 标题和正文都默认使用简体中文，不默认使用英文。
- 提交前必须确认工作区和暂存区范围：先查看 `git status --short` 和 staged diff，只暂存本次任务相关文件，避免混入用户或其他任务的改动；默认不要使用 `git add .`，除非已经确认整个工作区都属于本次提交。
- commit 粒度必须保持单一：一个 commit 只表达一个清晰目的；代码和对应方案文档属于同一行为变更时可以放在同一个 commit；格式化、重构和功能修改不要混在一起，除非格式化是本次改动的必要结果。
- 提交前必须尽量完成相关验证：涉及 Kotlin、Compose 或 Android 代码改动时，优先运行 `./gradlew :app:compileDebugKotlin` 或更贴近改动模块的编译/测试命令；如果因为耗时、环境或权限原因没有验证，必须在最终说明中明确说明。
- commit message 必须使用简体中文，标题用动宾短句直接说明改动对象和目的；范围明确时可以使用 `模块：动作和目的` 的格式，例如 `列表：修复切回时滚动回顶`。
- commit 不能只有一句标题，必须包含正文细则：至少列出关键改动点、已执行的验证命令和结果；如果存在风险、取舍、未验证项或后续注意事项，也必须在正文中说明。
- 禁止擅自改写历史或破坏工作区：不要主动执行 `git reset --hard`、`git checkout --`、`git rebase`、`git commit --amend`、强制推送等可能丢失或改写用户工作的操作；确实需要时必须先明确征得用户同意。
- 提交后必须检查结果：查看 `git status --short` 确认工作区是否干净，并用 `git log -1 --oneline --stat` 核对提交号和文件范围，再向用户汇报。
