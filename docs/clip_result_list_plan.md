状态：已完成

# 剪贴结果列表组件设计文档

## 当前状态

`ClipResultList` 是普通列表页、搜索结果页、折叠列表页和回收站列表页共同复用的剪贴结果列表组件，负责分页结果展示、空态/加载态、剪贴卡片结构、可选斜向快捷动作区、复制入口、可选右滑菜单、第二段滑动动作、关键词高亮、取消置顶后的视口修正和基础 item 测量。本次调整补充点击防误触规则：剪贴 item 只有在按下到抬起的最终位移没有超过系统 `touchSlop` 时才分发详情或快捷动作点击，快速滑动列表、斜向拖动或父级 Pager 接管手势时不应跳转详情。来源 App 名称展示已接入共享 `String?.toSourceAppDisplayName()`，空名称兜底规则以 `docs/source_app_display_plan.md` 为准。当前代码组织已将 `ClipResultList`、`ClipCardContent` 和 `ClipCardGestures` 收敛到 `app/src/main/java/com/cla/clip/master/ui/widget/clip/`，列表页、搜索页、折叠页和回收站只负责传入页面级数据、文案和业务回调。

本文是 `ClipResultList` 共享组件的主方案文档。后续涉及 `ClipResultList.kt`、共享卡片样式、侧滑方向、按钮布局、动画阈值、滚动修正、关键词高亮或跨页面列表结果复用的修改，都必须优先更新本文；页面文档只记录页面如何传入数据、动作语义和差异化文案。

## 目标

- 为普通列表、普通搜索、折叠列表、折叠搜索和回收站列表提供同一套剪贴结果展示体验。
- 保持剪贴记录操作清晰可点：普通列表和普通搜索可按用户设置启用左下斜向快捷动作区，快捷区执行复制/置顶/删除/折叠，其余区域进入详情；置顶、删除和复制也可通过右滑菜单露出。
- 支持第二段继续右滑动作，由页面传入具体语义：普通范围折叠数据，折叠范围取消折叠，回收站范围彻底删除。
- 支持普通时间、折叠时间和删除时间三种展示模式，保证可见时间和页面排序依据一致。
- 保持分页加载、空态、append 重试和切回列表后的滚动位置稳定。
- 将卡片结构、侧滑、动画、测量和滚动修正集中在组件内维护，避免页面文档和页面实现重复分叉。

## 范围

- `ClipResultList` 共享结果列表、空态、加载态、分页 append 和 append error 重试。
- `ClipCard` 卡片展示、来源色边框、链接预览、剪贴内容、来源 App、时间、置顶角标、斜向快捷动作区和复制入口。
- 可选右滑菜单、菜单按钮、第二段继续右滑提示、折叠/取消折叠/彻底删除离场动画和菜单吸附规则。
- 关键词高亮能力，供搜索页传入当前搜索词。
- 取消置顶后的视口修正，避免 `LazyColumn` 因稳定 key 自动跟随被移动 item。

不包含：

- 普通列表页的数据范围、标题、Shizuku 提示和搜索入口，详见 `docs/list_page_plan.md`。
- 搜索页的关键词输入、时间筛选、来源 App 筛选和路由范围，详见 `docs/search_page_plan.md`。
- 折叠状态的数据层、折叠入口和折叠搜索复用方案，详见 `docs/folded_clip_plan.md`。
- 回收站软删除、回收站页面、保留天数和自动清理方案，详见 `docs/recycle_bin_plan.md`。
- 首页 Pager、底部 Tab 和列表/我的页面切换策略，详见 `docs/main_page_plan.md`。

## 用户体验

- 结果使用单列竖向 item，不再使用瀑布流。
- item 保留卡片、边框和来源色；展示链接、链接标题、预览图、剪贴字符串、来源 App 和时间；来源 App 名称为空时通过共享显示名工具显示“未知”。
- 剪贴字符串最多展示三行，超出省略。
- 普通列表和普通搜索在用户快捷动作不是“无（整卡进入详情）”时启用左下斜向快捷动作区：以 item 左上角到底边中点连线作为边界，边界左下侧执行用户选择的动作，其余区域进入详情；整个 item 任意位置长按都触发现有长按回调，由页面决定是否启用长按动作。
- 详情或快捷动作点击必须是稳定轻点：按下到抬起的最终位移不超过系统 `touchSlop` 才会分发点击；轻微手抖仍按系统点击容忍处理，超过阈值的列表滑动、斜向拖动和关闭状态左滑只取消 item 点击，不跳转详情。
- 启用快捷动作且动作可执行时，快捷动作区在内容下方绘制与卡片边框同源的极浅三角背景（`appColor.copy(alpha = 0.02f)`），帮助用户识别区域但尽量不影响阅读；不画硬斜线，不显示背景中的复制图标。内容仍按整张 item 宽度铺满显示，不被快捷动作区挤压或改写测量。
- 快捷动作区、按压反馈和点击命中都基于同一套三角规则：`x < width / 2f && y > (height / (width / 2f)) * x`；斜线边界默认归详情区，降低删除/折叠等高影响动作误触风险。
- 用户选择“无（整卡进入详情）”、折叠列表、折叠搜索和回收站列表都不启用快捷动作区，不显示三角背景，整个 item 点击和长按保持页面传入的现有行为。
- item 向右拖动露出左侧可用操作按钮，普通范围和折叠范围通常包含复制、置顶/取消置顶和删除；回收站可关闭普通菜单按钮。向左拖动收回；列表内只允许一个 item 菜单保持展开，新 item 开始右滑时会自动收回上一个已展开菜单。
- item 在第一段右滑菜单基础上继续右滑时显示页面传入的提示文案，例如“继续右滑折叠数据”或“继续右滑取消折叠”。
- 继续右滑超过阈值并松手后，item 先完整滑出当前卡片区域，动画结束后再回调页面更新数据库，避免 Paging 刷新导致记录半途消失。
- 右滑菜单按钮顺序为复制、置顶/取消置顶、删除；按钮宽度统一为 48dp，图标尺寸统一为 24dp，并在各自点击区域内水平和竖向居中。
- 菜单按钮之间使用 0.5dp 固定高度分割线；分割线高度为按钮图标高度加 10dp，竖向居中，颜色与卡片边框一致。
- 置顶状态角标是卡片级视觉装饰，不响应点击；它位于卡片右上角并以极低透明度（`alpha = 0.18f`）展示，只做状态暗示，避免覆盖右上角剪贴文案时影响阅读。

## 数据和参数契约

- `pagedClips` 由页面传入，组件只消费 `LazyPagingItems<ClipShowEntity>`，不直接访问 Repository 或 ViewModel。
- `listState` 由页面持有，组件负责在取消置顶排序刷新后基于该状态恢复视口。
- 当前展开菜单 id 由 `ClipResultList` 父层维护，`ClipCard` 只上报自己开始展开或收回，避免多个 item 各自保存展开态后同时露出菜单。
- `emptyText` 由页面传入，用于区分普通列表、普通搜索、折叠列表、折叠搜索和回收站空态。
- `highlightQuery` 默认为空；搜索页传入当前关键词后，组件只在可见文本中高亮命中内容。
- `swipePastActionText` 为空时只允许普通菜单阶段；非空时开启第二段继续右滑提示和动作。
- `onSwipePastAction` 只表达第二段动作回调，具体是折叠还是取消折叠由页面决定。
- 删除、复制、置顶、详情点击、还原点击和多选点击继续通过回调传入，避免共享组件直接依赖某个页面 ViewModel。
- `ClipCard` 的 `pointerInput` 手势协程通过 `rememberUpdatedState` 读取最新 item 快照和页面回调，只解决手势协程捕获旧闭包的问题；页面级动画期间的点击命中和旧目的地保留不在共享组件内处理。
- 时间展示由页面传入展示模式；普通范围显示原剪贴时间，折叠范围显示折叠时间，回收站范围显示删除时间。
- 快捷动作由页面读取 `AppSetting.clipItemQuickActionFlow` 后传入；共享组件只负责按参数识别斜向区域和回调，不直接访问设置中心。普通范围支持复制、置顶、删除、折叠和无；折叠范围与回收站不启用快捷动作区。
- 来源 App 名称展示调用 `String?.toSourceAppDisplayName()`，组件只传入拍平后的来源名称，由共享扩展读取资源化“未知”兜底；完整空名称兜底规则以 `docs/source_app_display_plan.md` 为准。

## 组件结构

- `ClipResultList`
  - 空状态：`EmptyScreen`
  - 首次 refresh loading：居中 `CircularProgressIndicator`
  - 正常数据：`LazyColumn`
  - append loading：底部小 loading
  - append error：底部可点击重试文本
- `ClipCard`
  - 不裁剪阴影的外层侧滑容器
  - 按卡片圆角裁剪的左侧侧滑操作区和继续滑动提示层
  - 可横向拖动的 `ElevatedCard`
  - 卡片级最上层置顶角标装饰层
  - 可选斜向快捷动作区背景、完整宽度内容层和统一手势入口
- 辅助 UI
- `ClipCardContent.kt`
  - `SourceAppNameWithTime`
  - `ClipContent`
  - `LinkPreviewContent`
  - `SwipeActionButton`
  - `HighlightableText`
  - `EmptyScreen`
- `ClipCardGestures.kt`
  - `ClipCardPressedZone`
  - `detectClipCardGestures`
  - 快捷动作区、详情区和整卡路径计算

## 交互规则

- 复制：右滑菜单第一项调用 `onCopy(clip)`；普通范围快捷动作设置为“复制”时，点击左下斜向快捷动作区也调用 `onCopy(clip)`。
- 删除：右滑后点击删除按钮调用 `onDelete(clip)`，是否弹窗确认以及移入回收站/彻底删除由页面处理。
- 置顶/取消置顶：右滑后点击置顶按钮调用 `onPinToggle(clip)`；取消置顶前组件会记录当前视口，等待 Paging 排序刷新后恢复。
- 详情：未启用快捷动作区时，点击整个 item 调用 `onClick(clip)`；启用快捷动作区时，点击斜向快捷动作区之外的区域调用 `onClick(clip)`。
- 长按：长按 item 任意位置调用 `onLongClick(clip)`，当前是否有可见反馈由页面决定。
- 快捷动作：普通列表和普通搜索支持复制、置顶/取消置顶、删除、折叠和无；“删除”只打开现有删除选择弹窗，不静默删除；“折叠”只在普通范围生效，不在折叠范围反向取消折叠；“无（整卡进入详情）”完全关闭快捷动作区且不影响右滑菜单。
- 点击和侧滑统一在同一个手势入口协调：横向超过 touch slop 且符合右滑/菜单收回时进入侧滑流程；任意方向总位移超过 touch slop 但不属于 item 侧滑时取消点击，交给列表滚动或首页 Pager；即使快速滑动后首个可见事件已经是抬手事件，也要按最终位移兜底取消点击；未超过 touch slop 并正常抬手时再按斜向公式分发快捷动作或详情；到达长按时间且未超过 touch slop 时触发长按并取消点击。
- 按压态只反馈实际命中的区域：点快捷动作区时只反馈三角区，点详情区时不让左下角误亮；右滑、纵向滚动、长按触发、手势 cancel 和 item 复用到新 `clip.id` 时都必须清理按压态。
- 第二段动作：右滑露出菜单后继续右滑，或在无菜单模式下直接右滑，超过阈值并松手后播放离场动画，再调用 `onSwipePastAction(clip)`。
- 多选：页面可传入选中 id 集合，组件只展示选中态并把点击/长按回调交回页面。
- 侧滑：
  - 只允许向右展开、向左收回。
  - 新 item 开始右滑时会登记为当前展开菜单，原已展开 item 监听到菜单归属变化后自动收回。
  - 关闭状态下的左滑不消费事件，交给外层首页 Pager 切到“我的”。
  - 菜单已展开或动画中时，左滑优先由 item 消费用于收回菜单，避免展开状态直接切页。
  - 普通菜单阶段最大偏移等于左侧操作区宽度。
  - 存在第二段动作时最多可拖到完整 item 宽度，触发阈值为 item 宽度 85%；无菜单的回收站 item 也沿用该阈值。
  - 松手时按操作区一半宽度吸附；未达到第二段触发阈值时使用较长吸附动画回到菜单或收起状态。
  - 侧滑偏移按 `clip.id` 使用 `rememberSaveable` 保存，父层额外保存当前展开菜单 id，避免 LazyColumn 复用导致状态串到其他记录，同时保证最多只有一个菜单可见。

## 高亮规则

- 高亮只处理用户看得见的字段，匹配词由搜索页通过 `highlightQuery` 传入。
- 当前高亮范围包含剪贴内容 `content`、链接预览标题 `linkTitle` 和来源 App 名称 `appName`。
- 关键词会先去除首尾空白，按空白拆词、去重并做大小写不敏感匹配。
- 多个词重叠时只给尚未覆盖的位置加样式，避免重复添加 Span 造成边界不可预测。
- 普通列表和折叠列表传空关键词，不额外分配高亮片段。

## 高度和测量

- 卡片内容高度由完整宽度内容层真实测量结果决定。
- 不再使用 `IntrinsicSize.Min + fillMaxHeight` 让按钮反向参与高度测量，避免置顶重排后小 item 继承旧首条大 item 高度。
- `ClipCard` 不再为复制入口或快捷动作区扣除宽度；三角背景和统一手势入口都使用卡片最终高度跟随内容，不参与内容测量。
- 复制入口只存在于右滑菜单和可选快捷动作中，不再使用背景图标；菜单分割线仍按固定高度竖向居中，不拉满卡片高度。
- 左侧侧滑操作区通过外层 `matchParentSize()` 跟随最终 item 高度。

## 生命周期和性能约束

- 组件不主动创建分页流，不在组件内访问数据库；分页生命周期由页面通过 `flowWithLifecycle`、`collectAsLazyPagingItems` 等方式控制。
- `refresh Loading && itemCount == 0` 时展示独立 loading，不组合绑定同一个 `LazyListState` 的空 `LazyColumn`，避免空刷新窗口把滚动状态钳回顶部。
- Paging key 使用数据库主键，保证置顶、删除、搜索条件变化和侧滑状态保存时 Compose 复用稳定。
- 普通列表、普通搜索、折叠列表、折叠搜索和回收站的 DAO 分页查询必须在业务排序字段之后追加 `id DESC` 作为唯一兜底排序键；如果只按时间、置顶或命中质量排序，同毫秒记录可能在 Paging 分页边界重复出现，导致 `LazyColumn` 收到重复 `clip.id` key。
- 共享列表还要基于 `LazyPagingItems.itemSnapshotList` 做一次快照级 `clip.id` 去重，只保留当前已加载快照里首个出现的条目；原因是 Paging 在数据重排、失效切代或旧页回收边界上，仍可能短暂把同一条记录暴露两次，UI 需要先兜住重复 key 崩溃，再等待下一次稳定快照覆盖。
- 如果调整 item key、contentType、refresh 空态或侧滑状态保存方式，必须验证置顶、删除、搜索条件变化、切回页面、侧滑状态和滚动位置。

## 日志与诊断计划

- 本组件的点击防误触调整属于纯本地手势分发，不访问网络、文件、数据库或系统权限，不新增运行日志。
- 手势移动距离、剪贴内容、搜索词和来源 App 不写入日志，避免高频交互噪声和隐私泄露。
- 诊断方式以人工手势验证和编译验证为主：确认轻点仍进入详情，列表滑动和斜向短拖不会误进详情，右滑菜单与第二段继续右滑仍按既有阈值触发。

## 涉及文件

- `app/src/main/java/com/cla/clip/master/ui/widget/clip/ClipResultList.kt`
- `app/src/main/java/com/cla/clip/master/ui/widget/clip/ClipCardContent.kt`
- `app/src/main/java/com/cla/clip/master/ui/widget/clip/ClipCardGestures.kt`
- `app/src/main/java/com/cla/clip/master/ui/page/list/ClipListPage.kt`
- `app/src/main/java/com/cla/clip/master/ui/page/list/FoldedClipListPage.kt`
- `app/src/main/java/com/cla/clip/master/ui/page/search/SearchPage.kt`
- `app/src/main/java/com/cla/clip/master/ui/page/recycle/RecycleBinPage.kt`
- `docs/clip_result_list_plan.md`
- `docs/list_page_plan.md`
- `docs/search_page_plan.md`
- `docs/folded_clip_plan.md`
- `docs/recycle_bin_plan.md`

## 测试验证

- 编译验证：涉及组件代码改动时运行 `./gradlew :app:compileDebugKotlin`；本次来源 App 显示名共享工具接入已运行并通过。
- item 展示验证：无链接、仅链接、仅标题、仅预览图、长剪贴字符串、置顶 item、不同来源色 item 都能正确展示。
- 点击验证：普通范围设置为复制、置顶、删除、折叠时斜向快捷动作区背景可见且动作正确，其余区域进入详情，任意位置长按回调正确；设置为“无（整卡进入详情）”时整卡轻点进入详情且右滑菜单仍可用；按住后快速上下滑、斜向短拖或关闭状态左滑松手不会进入详情。
- 高亮验证：剪贴内容、链接标题、来源 App 名称中命中的关键词能高亮；空关键词不高亮。
- 侧滑验证：右滑露出操作，左滑收回；先展开 A 再右滑 B 时，A 会自动收回，列表最多只有一个普通菜单可见。
- 第二段动作验证：继续右滑出现页面传入提示，超过阈值松手后 item 先完成离场动画，再由页面更新数据范围。
- 折叠验证：折叠列表和折叠搜索不显示斜向背景，不启用快捷动作区，保留置顶/取消置顶按钮，整卡点击进入详情，右滑第二段仍可取消折叠；卡片时间展示为折叠时间。
- 回收站验证：无复制入口和普通菜单按钮时，不启用快捷动作区，整卡点击还原，继续右滑仍能触发彻底删除；卡片底部显示删除时间。
- 手势回调验证：同一列表组件在折叠页、搜索页和回收站复用时，点击、长按、右滑菜单和第二段侧滑应调用当前页面传入的最新回调，不应执行手势协程曾经捕获的旧闭包。
- 右滑菜单验证：按钮顺序为复制、置顶/取消置顶、删除；复制按钮颜色为 `MaterialTheme.colorScheme.primary`；三个按钮时吸附距离随菜单宽度增加后仍顺手。
- Pager 手势验证：item 未展开时列表页左滑可以切到“我的”，item 已展开时左滑优先收回菜单。
- 高度验证：高 item 后面置顶矮 item、取消置顶矮 item，都应按自身内容包裹高度显示。
- 取消置顶滚动验证：取消置顶当前可见 item 后，列表视口停留在操作时的位置，不跟随该 item 回到原排序位置。
- DAO 排序回归验证：同一置顶状态、同一时间戳、同一折叠时间或同一删除时间的数据，应按 `id DESC` 稳定输出，避免 Paging 对同一条剪贴记录产生重复 key。
- 快照去重验证：模拟同一 `itemSnapshotList` 中短暂出现重复 `clip.id` 的情况，组件应只渲染首个真实索引，不因重复 key 崩溃，也不把后续快照的占位偏移映射错位。

## 已知取舍

- 共享组件只负责展示和回调，不直接决定普通列表、搜索或折叠列表的数据范围。
- 取消置顶后恢复的是操作时的 index/offset 视口，不保证继续显示同一条内容；这是为了满足“视口不动”的体验。
- 如果取消置顶同时发生新增、删除或后台刷新，恢复视口可能有轻微偏差，但会通过 index 边界保护避免崩溃。
- 右滑菜单采用单展开模式；当前展开 id 保存在 `ClipResultList`，如果已展开 item 因分页刷新离开可见快照，父层 id 可能暂时保留，但不会展示多个菜单，下一次展开会覆盖该 id。
- 第二段动作由页面传入，普通范围折叠，折叠范围取消折叠，回收站范围彻底删除；组件不理解具体业务，只负责触发时机、无菜单模式、选中态和离场动画。
- `ClipCard` 外层侧滑容器不做圆角裁剪，避免裁掉公共卡片阴影；右滑菜单和继续滑动提示层各自按同一 `cardShape` 裁剪，以保留圆角背景和阴影两种效果。

## 开放问题

- 是否需要点击列表空白处或页面切换时自动收起当前展开菜单。
- 是否需要为共享 item 增加更明确的可访问性动作，例如“查看详情”和“执行快捷动作”，减少 TalkBack 用户对几何热区的依赖。
- 是否恢复长按底部弹窗，或改为更明确的更多操作入口；如果恢复，需要重新评估和侧滑、详情点击的冲突。

## 本次同步

- `ClipCard` 已接入 `ClipMasterGestureCard` 公共内容卡片外壳，统一圆角、阴影、边框绘制位置和外壳裁剪。
- `ClipCard` 仍保留来源 App 色边框、快捷动作区底色、快捷动作按压色、详情按压色、侧滑菜单分隔线和全部侧滑/长按手势；公共外壳不覆盖这些业务状态色，完整规则详见 `docs/shared_card_component_plan.md`。
- `ClipCard` 已将圆角裁剪从整个 item 外层下沉到侧滑菜单和继续滑动提示层，确保剪贴列表、搜索结果、折叠列表、折叠搜索结果和回收站能显示公共卡片阴影。

## 变更记录

- 2026-05-28：补充 `ClipResultList` 的 Paging 快照级 `clip.id` 去重防御；原因是即使 DAO 已按 `id DESC` 稳定排序，Paging 在数据重排或切代边界上仍可能短暂暴露重复记录，需先在共享列表层兜住 `LazyColumn` 重复 key 崩溃。
- 2026-05-26：补充剪贴分页 DAO 必须以 `id DESC` 作为最终兜底排序键；原因是同一 LazyColumn 内重复 `clip.id` key 会导致 Compose 崩溃，稳定排序应由数据层保证而不是用 UI index 掩盖。
- 2026-05-17：修正 `ClipCard` 阴影被外层裁剪的问题，将圆角裁剪下沉到菜单和继续滑动提示层；原因是剪贴列表、搜索结果、折叠列表、折叠搜索结果和回收站复用该 item，整个 item 裁剪会遮掉公共卡片阴影。
- 2026-05-17：`ClipCard` 接入 `ClipMasterGestureCard` 公共内容卡片外壳，同时保留来源色边框、快捷动作区域颜色、按压反馈颜色和侧滑交互；原因是剪贴列表、回收站、折叠页和搜索结果需要统一主要内容卡片外壳，但不能丢失现有业务状态色。
- 2026-05-15：新增 `ClipResultList` 共享组件设计文档；原因是普通列表、搜索结果和折叠列表都复用同一套结果 item，组件级卡片结构、侧滑交互、动画阈值、测量和滚动修正不应继续放在列表页文档中重复维护。
- 2026-05-15：记录右滑菜单、第二段继续右滑、85% 触发阈值和离场后更新数据的当前实现；原因是共享组件已经承担跨页面一致的剪贴 item 操作体验。
- 2026-05-15：补充回收站列表复用约束；原因是回收站需要复用卡片展示和右滑离场动画，但关闭复制/置顶/普通删除菜单，并显示删除时间和多选选中态。
- 2026-05-15：将常驻复制按钮从卡片右侧调整到卡片左侧；原因是侧滑菜单从左侧展开，复制入口同侧能降低视觉割裂，回收站无复制按钮场景保持不变。
- 2026-05-15：将置顶角标调整为卡片内部最高装饰层；原因是复制按钮移动到左侧后，卡片内容层和选中态可能盖住角标，需要明确角标最后绘制并高于其他兄弟节点。
- 2026-05-15：将侧滑菜单从多 item 可同时展开调整为单 item 展开；原因是列表中多个菜单同时露出会增加视觉噪音和误操作风险，新 item 开始右滑时自动收回上一个菜单。
- 2026-05-16：曾将左侧 48dp 独立复制按钮调整为底层复制视觉区和透明热区；原因是当时希望剪贴内容铺满整张 item，同时保留左侧复制、右侧详情和整卡长按边界；该视觉方案已被后续左右等分方案取代。
- 2026-05-16：曾尝试调淡左侧复制视觉区底色；原因是内容铺满 item 后旧底色偏重会削弱左侧文本可读性；最终不再保留背景图标方案。
- 2026-05-16：将上一版左侧复制视觉彻底替换为普通范围左右等分点击区，并新增右滑菜单复制按钮和左半区动作设置；原因是背景图标方案视觉效果不佳，需要让用户可选择左半区动作或完全关闭分区。
- 2026-05-17：将左半区分区背景从边框同透明度调整为更低透明度的同源色；原因是左右等分后左半区面积较大，原背景观感偏重，会干扰剪贴内容阅读。
- 2026-05-17：继续将左半区分区背景透明度降低到 `0.02f`；原因是 `0.1f` 在部分来源色下仍然偏明显，需要进一步弱化背景存在感。
- 2026-05-17：将左右等分快捷点击改为左下斜向快捷动作区，并同步重命名设置契约为 `ClipItemQuickAction` / `clipItemQuickActionFlow`；原因是左右二分会把 item 视觉和点击语义切得过硬，斜向区域能让详情成为主要点击路径，同时保留快捷操作效率。
- 2026-05-17：将右上角置顶角标透明度降低到 `0.18f`；原因是角标只需要提示置顶状态，不能遮挡或干扰剪贴文案阅读。
- 2026-05-17：将共享组件文档标记为实现中，补充 `FoldedTime` 展示模式和折叠范围置顶入口调整；原因是折叠列表和折叠搜索需要展示折叠时间。
- 2026-05-17：完成 `FoldedTime` 展示模式接入并将状态更新为已完成；原因是折叠列表和折叠搜索已通过共享组件展示“折叠于 X”。
- 2026-05-17：将折叠范围置顶入口调整标记为实现中，恢复折叠列表和折叠搜索向共享组件传入置顶回调；原因是用户确认折叠页面仍需要保留置顶功能。
- 2026-05-17：修正折叠范围置顶排序说明；原因是用户进一步确认折叠页面应先显示置顶数据，再按折叠时间倒序，组件只展示入口和时间，排序由数据层负责。
- 2026-05-17：完成折叠范围置顶入口和折叠时间展示说明同步并将状态更新为已完成；原因是共享组件继续接收置顶回调，排序规则由折叠范围数据源负责，并通过 `./gradlew :app:compileDebugKotlin` 验证。
- 2026-05-17：恢复共享卡片手势协程读取最新页面回调的处理，并明确其边界；原因是 `pointerInput` 协程捕获旧闭包属于共享组件自身正确性问题，应与页面级点击穿透治理分开处理。
- 2026-05-18：剪贴 item 来源 App 名称接入 `String?.toSourceAppDisplayName()`；原因是来源 App 名称会在多个页面展示，空名称显示“未知”的规则需要与搜索来源选择共用同一套方法。
- 2026-05-18：按当前代码组织规则拆分共享列表组件，新增 `ClipCardContent.kt` 和 `ClipCardGestures.kt`；原因是 `ClipResultList.kt` 同时承担列表状态、卡片主流程、内容展示、高亮和底层手势判断，拆出纯展示与纯手势能力后更符合共享组件主文档的职责边界。
- 2026-05-18：将 `ClipResultList`、`ClipCardContent` 和 `ClipCardGestures` 从 `ui/page/list` 迁移到 `ui/widget/clip`；原因是该组件已经被列表页、搜索页、折叠页和回收站复用，不应继续放在列表页私有目录下。
- 2026-05-21：补充剪贴 item 点击防误触规则并完成实现；原因是用户反馈滑动剪贴列表时容易误触详情，需要在共享手势层按最终位移兜底取消点击，同时保留正常轻点进入详情。
