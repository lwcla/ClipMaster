状态：已完成

# 回收站功能方案

## 当前状态

剪贴数据已从“删除即从数据库移除”调整为“可选择移入回收站或彻底删除”。回收站是剪贴数据的软删除范围，只保存 `clips` 表中的删除时间，不复制内容，也不清理来源 App 和链接预览缓存。

本文是回收站能力主方案文档。后续涉及回收站入口、软删除/硬删除、回收站分页、保留天数、自动清理、删除弹窗、回收站 item 交互或相关数据层查询时，必须优先更新本文；列表页、搜索页、折叠页和共享列表文档只记录接入差异。

## 目标

- “我的”页新增回收站入口，并显示 `deleted_at > 0` 的剪贴数据数量。
- 其他剪贴页面删除时提供“移入回收站”和“彻底删除”两个动作。
- 回收站页分页展示已删除数据，按删除时间倒序排列，并显示“删除于 X”。
- 回收站支持还原、单条右滑彻底删除、长按多选彻底删除、清空全部、保留天数设置和过期自动清理。
- 普通列表、搜索、折叠列表、详情和剪贴采集去重都不读取回收站数据。

## 数据与接口

- `ClipData.deletedAt` 持久化删除时间，单位毫秒；`0` 表示正常数据，大于 `0` 表示在回收站。
- 数据库版本升到 7，迁移 6 -> 7 增加 `deleted_at INTEGER NOT NULL DEFAULT 0`，并创建 `deleted_at` 单列索引和 `deleted_at + is_folded` 组合索引。
- 普通/折叠分页、搜索、详情、折叠数量、最新记录和重复内容查询全部添加 `deleted_at = 0` 过滤。
- 回收站记录不参与去重；删除后重新复制同样内容会创建新的正常记录。
- Repository 明确拆分软删和硬删接口：`moveClipToRecycleBin`、`deleteClipPermanently`、`restoreClipFromRecycleBin`、`loadRecycleBinClips`、`observeRecycleBinCount`、`clearRecycleBinPermanently` 和 `cleanupExpiredRecycleBinClips`。
- 批量软删/硬删使用事务并分块处理 id；同一批移入回收站使用同一个 `deleted_at = now`，回收站分页最终按 `deleted_at DESC, timestamp DESC, id DESC` 排序，保证同批删除和同时间戳数据也有唯一稳定顺序。
- 清空回收站和过期清理使用条件 SQL，不先读取全部 id；批量操作对已不存在 id 保持幂等。
- 彻底删除只删除 `clips` 行，暂不清理 `SourceApp` 和 `LinkPreview` 孤儿缓存，避免误删仍被其他剪贴复用的缓存。

## 用户体验

- “我的”页权限分组在最上方，数据管理分组中的入口顺序为备份与恢复、下载记录、折叠数据、回收站；回收站入口显示“回收站内有 X 条剪贴数据”。
- 其他剪贴页面的删除确认弹窗统一使用 `ClipDeleteChoiceDialog`，“移入回收站”为主动作，“彻底删除”为危险动作，并提示彻底删除后不可恢复。
- 回收站页标题为“回收站”，右上角显示清空和设置按钮；清空需要二次确认，确认后彻底删除回收站全部数据。
- 回收站页已接入 `SecondaryPageScaffold` 二级页面骨架，标题栏、页面背景和多选底部栏跟随本轮固定品牌主题；回收站还原、彻底删除、清空和设置逻辑不变。
- 回收站空态显示“回收站是空的”。
- 回收站列表复用 `ClipResultList` 卡片，但隐藏复制、置顶和普通删除按钮；点击 item 只弹出还原确认，不跳详情页。
- 回收站普通态列表底部与其他剪贴结果列表保持一致，保留 12dp 轻量留白；多选态继续为底部彻底删除栏预留更大空间。
- 回收站 item 右滑到底直接彻底删除；长按进入多选，多选删除需要二次确认。
- 多选态拆分为独立的多选模式和选中 id 集合；反选最后一条数据只会让选中数量变为 0，不自动退出多选，必须由用户点击返回或完成删除后退出。
- 切换亮暗色、触发页面重建或 Paging refresh 时，多选态继续保留当前选中 id，把它当作用户正在编辑的临时草稿，而不是实时跟随列表快照修剪。
- 分页刷新、还原或后台清理导致某些 item 消失时，不在页面 refresh 过程中主动修剪选中集合；确认删除时由仓库层按幂等语义忽略已不存在的 id。
- 页面切换动画统一由 `AppNavigation` 管理，回收站自身不为动画额外改变点击、长按、右滑、清空、设置或弹窗逻辑；完整导航约束见 `docs/app_navigation_plan.md`。
- 设置底部弹窗提供 7 天、30 天和自定义天数，显示“当前保留：X 天”；自定义范围为 1 到 3650 天。
- 保存保留天数后立即按滚动时间窗口清理过期数据，例如 7 天表示 `deleted_at < 当前时间 - 7 * 24h`。
- 删除、还原、彻底删除、清空和页面内设置清理后给轻量 Toast 反馈；后台 Worker 静默清理，不发通知、不弹 Toast。

## 生命周期和清理

- 回收站列表使用 `Pager + PagingSource` 分页加载，并由页面在 `STARTED` 生命周期内收集。
- `MineVm` 只通过 COUNT Flow 展示回收站数量，不为了统计加载回收站分页。
- `AppSetting.recycleBinRetentionDays` 保存保留天数，默认 30 天。
- 主进程启动时调度每日 `RecycleBinCleanupWorker`，并立即执行一次过期清理。
- 保存设置后由回收站 ViewModel 立即执行一次清理并反馈结果；后台 Worker 只负责删除过期数据。

## 测试验证

- 运行 `./gradlew :app:compileDebugKotlin`，确认 Room 迁移、schema 7、KSP、Compose 和 Worker 注入编译通过。
- 验证旧库迁移后历史数据 `deleted_at = 0`，普通列表仍可见。
- 验证普通/折叠列表、搜索和详情不显示回收站数据，回收站数据不参与重复复制去重。
- 验证“我的”页回收站数量随移入、还原、彻底删除和清空实时变化。
- 验证列表、搜索、折叠页和详情页统一使用删除选择弹窗，两个动作分别进入回收站或彻底删除。
- 验证回收站分页、删除时间倒序、删除时间展示、空态、append loading 和 append retry。
- 验证还原、右滑彻底删除、多选彻底删除、清空和保留天数设置。
- 验证多选态反选最后一条记录后仍停留在多选模式，底部彻底删除按钮禁用，返回键或左上角返回才退出多选。
- 验证回收站多选状态下选中若干 item 后切换亮暗色，已选中的 id 仍然保留。
- 验证回收站进入和返回时跟随 `AppNavigation` 呈现完整左进右出页面动画。
- 验证大量 id 批量操作不触发 SQLite 参数数量限制，部分 id 已不存在时不报错。
- 验证启动清理、每日 Worker 和保存设置后的立即清理只删除过期回收站数据。
- 验证清空、设置、彻底删除和还原等入口均有资源化无障碍文案。

## 已知取舍

- 第一版不做撤销、回收站搜索和批量还原；后续如需要应单独设计。
- 还原只清零 `deleted_at`，不更新时间戳，保留原折叠状态、置顶状态、内容、来源和时间。
- 当前没有清空入口的页面不强行新增清空；后续新增时复用本方案的双动作确认弹窗。
- 硬删除暂不做来源 App 和链接预览孤儿缓存清理；后续如需存储瘦身，单独设计缓存清理方案。

## 开放问题

- 是否需要后续提供回收站搜索。
- 是否需要后续提供批量还原。
- 是否需要后续提供删除后的撤销操作。

## 本次同步

- 回收站列表通过 `ClipResultList` 接入 `ClipMasterGestureCard` 公共内容卡片外壳；本页面只记录回收站接入影响，卡片外壳规则以 `docs/shared_card_component_plan.md` 为准，剪贴 item 状态色和回收站删除/还原交互以 `docs/clip_result_list_plan.md` 为准。
- 回收站保留天数弹层的单选行复用 `SingleChoiceRow`；完整弹层仍留在回收站页面，因为保留范围、保存按钮、立即清理和自定义天数校验属于回收站业务。共享表单组件规则见 `docs/shared_form_components_plan.md`。
- 回收站在 UI 刷新中复用 `SecondaryPageScaffold`；多选状态标题、清空/设置动作和底部彻底删除栏仍由回收站页面维护，因为它们直接绑定回收站选择状态和危险操作确认。
- 回收站多选“彻底删除”按钮使用 `DeleteActionIcon` + 文字，纳入删除类入口统一；“清空回收站”继续使用 `DeleteForever` 清空语义图标并统一危险色，二次确认弹窗保持 Material 文字按钮形态。

## 变更记录

- 2026-06-08：同步回收站删除/清空入口视觉规则；原因是多选彻底删除属于删除类入口，清空回收站属于清空类入口，需要保留不同语义但同属危险色。
- 2026-05-28：移除回收站多选态的实时 `pruneSelection`；原因是当前多选 UX 只支持删除和退出，多选更适合作为用户临时草稿保留，不需要在 Paging refresh 过程中持续修剪选中集合。
- 2026-05-28：按确认结果恢复回收站普通态 12dp 底部留白；原因是 8dp 在当前卡片高度和底部栏关系下偏紧，普通态需要和其他结果列表继续保持同一节奏。
- 2026-05-28：继续收细回收站普通态列表底部留白；原因是普通剪贴结果列表的统一底部节奏再次收口，回收站普通态也需要同步到同一档位；多选态仍为底部操作栏让位。
- 2026-05-28：统一回收站普通态列表底部留白；原因是回收站普通态同样复用 `ClipResultList`，需要和普通列表、折叠列表、搜索结果保持一致的底部节奏；多选态仍单独为底部操作栏预留空间。
- 2026-05-27：回收站页接入统一二级页面骨架；原因是本轮 UI 刷新要求二级管理页统一标题栏、背景和 bottomBar 入口，同时保持回收站分页、还原、多选和彻底删除语义不变。
- 2026-05-26：补充回收站分页查询的 `id DESC` 最终排序键；原因是同批删除数据可能共享 `deleted_at` 和 `timestamp`，需要唯一顺序避免共享列表重复 key。
- 2026-05-17：记录回收站列表经由 `ClipResultList` 接入公共内容卡片外壳；原因是回收站复用共享剪贴 item，需要统一主要内容卡片外壳，同时保留回收站特有的还原、多选和彻底删除语义。
- 2026-05-18：回收站保留天数选项行接入 `SingleChoiceRow`；原因是单选行是跨页面通用 UI，完整保留天数弹层仍由回收站维护保存和清理副作用。
- 2026-05-15：新增回收站功能方案并标记为实现中；原因是剪贴删除语义、数据库结构、回收站入口、分页页面和自动清理都将发生变化，需要主文档承载完整设计。
- 2026-05-15：完成回收站功能并将状态更新为已完成；原因是数据库迁移、软删/硬删接口、回收站分页页面、我的页入口、删除选择弹窗、保留天数设置和自动清理 Worker 已落地，并通过 `./gradlew :app:compileDebugKotlin` 验证。
- 2026-05-15：调整回收站多选状态机；原因是反选最后一条数据时不应被动退出多选，需将多选模式和选中 id 集合分离，让退出多选成为用户明确动作。
- 2026-05-17：补充回收站页面前台生命周期交互保护；原因是导航退出过渡期旧页面仍可能短暂保留组合树，需关闭本地弹窗并忽略延迟点击，避免进入折叠数据等页面后偶发出现还原确认。
- 2026-05-17：记录应用导航层将默认淡入淡出改为轻量横向进入动画后的回收站返回约束；原因是仅在回收站页忽略非前台点击会把过渡期触摸吞掉，完整双页滑动又会增加重页面重绘压力，最终需要避免旧页面原地覆盖并降低切换卡顿。
- 2026-05-17：将回收站返回约束同步为元素联动动画和 `RESUMED && !exiting` 交互保护；原因是导航层过渡仍可能让旧目的地参与触摸命中，需让回收站只播放自身退出动画并在退出期禁用本页高风险操作。
- 2026-05-17：将回收站返回约束同步为 Compose Navigation 官方左进右出页面级转场和 `RESUMED` 交互保护；原因是元素联动观感不佳，需要试验官方容器转场下旧问题是否仍复现。
- 2026-05-17：将回收站导航约束同步为进入页滑动、退出页无动画；原因是仅限制回收站业务点击属于局部兜底，其他退出页面仍可能挡住新页面点击，需要由导航层全局缩短旧目的地命中窗口。
- 2026-05-17：按用户要求回退回收站页面内点击穿透防护说明，回收站不再为页面动画额外改变本页交互；原因是本次目标收敛为只增加完整左进右出页面切换动画。
