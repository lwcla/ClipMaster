# AGENTS.md

## Plan Documentation Convention

- 后续所有方案设计、需求澄清、实现计划、架构调整、代码修改和行为变更，都必须同步维护本地方案文档，确保文档与最新讨论和代码保持一致。
- 方案文档应优先保存在 `docs/` 目录；如果某个功能已经有对应文档，后续修改必须优先更新原文档，避免创建重复、分叉或互相矛盾的说明。
- 修改代码前如果涉及既有方案变化，必须先或同步更新对应方案文档，记录变更原因、影响范围、数据/接口/UI 调整、边界条件和后续待办。
- 修改代码后如果实现与方案存在偏差，必须同步修正文档，说明最终采用的实现、舍弃的方案以及取舍原因，避免文档停留在过期设计。
- 新增方案文档时，需要写清楚当前状态、目标、范围、用户体验、数据流、涉及文件、实现步骤、测试验证、已知取舍、开放问题和变更记录；后续小改动也要更新“变更记录”或相应章节。
- 每个方案文档顶部必须标注当前状态，状态限定为 `草案`、`已确认`、`实现中`、`已完成`、`已废弃` 中的一种；当需求确认、开始实现、完成实现或废弃方案时，必须同步更新状态。
- 每次修改方案文档时，必须在“变更记录”中追加日期、改动内容和改动原因，方便后续追踪需求变化和实现取舍。
- 如果发现方案文档与当前代码行为或最新需求冲突，必须立即修正文档或代码，并在文档中说明最终以哪个行为为准，不能让冲突长期保留。
- 如果代码实现偏离原方案，必须在对应文档中补充最终实现、未采用方案和取舍原因，确保文档描述的是当前真实设计。
- 每次完成任务前必须把“是否需要更新方案文档”作为收尾检查项；如果相关需求、方案或代码发生变化，就必须同步更新对应文档。
- 尚未确定的需求点、交互细节、性能取舍或后续增强，必须集中记录在方案文档的“开放问题”或“后续待办”章节，避免散落在正文中造成误解。
- 当前搜索页方案文档位于 `docs/search_page_plan.md`，后续搜索入口、搜索筛选、搜索结果高亮、列表结果复用或相关数据层改动，都必须同步更新该文档。

## Page Lifecycle and Performance Convention

- 编写页面时必须优先从页面生命周期出发设计数据收集、任务启动、媒体读取、分页加载、传感器/定位、WebView、后台轮询和其他重型操作，确保页面进入可见/前台状态时才开始，页面离开可见/前台状态时能及时暂停或取消，避免不可见页面继续消耗 CPU、IO、内存、网络或电量。
- Compose 页面收集 Flow、Paging、数据库观察、权限状态、服务连接和一次性事件时，应优先使用 `collectAsStateWithLifecycle`、`flowWithLifecycle`、`repeatOnLifecycle` 或等价生命周期感知方案；不要在 ViewModel 初始化阶段默认启动会读取大量数据、扫描本地媒体、执行网络请求或持续观察数据库的任务，除非该任务确实与页面可见性无关并已说明原因。
- Tab、筛选条件、Pager 页码、SegmentedButton 等页面内控件只能表达“当前用户选择查看什么内容”，不能替代页面生命周期来决定“页面是否应该工作”；切换 Tab 可以决定收集哪一路数据，但数据流的启动、暂停和取消仍必须受页面生命周期约束。
- 多 Tab、多页面或横向 Pager 场景中，应避免因为默认选中项而同时加载所有分类数据。默认只加载当前可见页面或当前 Tab 必需的数据；切换到其他 Tab 后再按需加载对应数据，并在离开当前 Tab 或页面不可见时停止不必要的观察和重型处理。
- 面对可能增长的数据集合，优先使用分页、分批、懒加载或按需查询；标题栏数量、全选、清空、运行中状态等全局信息应使用轻量 `COUNT`、id 查询或专用聚合接口，不应依赖已经加载的可见列表，也不应为了统计而一次性加载全部实体。
- 涉及页面生命周期、分页加载、按需加载、缓存策略或重型任务启停的实现，必须在对应方案文档和代码注释中说明触发时机、取消边界、性能取舍以及为什么放在页面层或 ViewModel 层，避免后续维护时把生命周期约束误改成控件状态驱动。

## Code Commenting Convention

- 后续新增或修改代码时，必须同步补充简体中文注释，注释重点说明“做什么”“为什么这样做”“有什么边界/代价”，而不是复述语法。
- 新增或修改的类、接口、枚举、密封类、数据类、Worker、ViewModel、Repository、DAO、工具类等类型，都需要添加尽量详细的简体中文注释，说明职责、使用场景、生命周期、线程/协程约束、跨模块契约以及必要的设计意图。
- 新增或修改的方法、构造函数、扩展函数、Composable、回调、协程任务、Worker 入口和工具函数，都需要添加简体中文注释，说明输入输出、核心流程、调用时机、异常/失败处理以及为什么放在当前层级实现。
- 方法注释要求不区分可见性，`public`、`internal`、`protected`、`private` 方法和本文件内的辅助函数都必须按同一标准补充简体中文注释，不能因为只在局部使用就省略说明。
- Compose 相关注释要求覆盖所有 `@Composable` 函数，包括页面入口、私有 UI 片段、布局容器、状态分支、Preview、remember 辅助函数和格式化展示函数；需要说明它承载的 UI 职责、状态输入、用户交互、重组/状态保存边界以及为什么拆成当前函数。
- 新增或修改的变量、常量、配置项、阈值、状态字段、缓存字段、锁、集合、正则、JS 脚本、SQL 片段、Intent extra key、WorkManager key、Bundle key、Preference key、数据库字段名等，都需要添加简体中文注释，说明用途、单位、取值原因、业务含义、有效范围和修改风险。
- 每个实体类的字段都需要添加简体中文注释，说明字段的作用、业务含义、取值限制、是否可空、默认值含义、持久化/序列化约束以及修改时可能影响的读写方。
- 涉及关键分支、提前返回、兜底逻辑、边界条件、重试、超时、去重、排序、过滤、限流、并发控制、线程切换、资源释放、权限判断、版本兼容、存储路径、媒体库行为、网络请求头、Cookie/UserAgent/Referer、反盗链处理、数据迁移、数据库事务、幂等性和非直观实现时，必须补充简体中文注释说明原因。
- 涉及 UI 状态、页面生命周期、Compose 状态保存、副作用、DisposableEffect/LaunchedEffect、WebView 生命周期、JS 注入、DOM 扫描、用户可见文案、通知、下载进度和失败展示时，需要注释说明状态流转和用户体验上的考虑。
- 涉及安全、隐私、权限、文件读写、外部存储、后台任务、前台服务、跨进程、Shizuku、系统 API、反射、隐藏 API 或平台版本差异时，需要注释说明风险、兼容策略和为什么需要这样处理。
- 修改既有逻辑时，如果改动改变了原有行为、默认值、排序规则、过滤规则、保存位置、错误处理或兼容范围，需要在相关位置补充注释说明行为变化和原因。
- 对复杂算法、解析规则、正则表达式、启发式判断、魔法数字、性能优化、内存优化和采样策略，需要注释说明思路、依据、适用范围和可能的误判/取舍。
- 对跨文件或跨模块调用形成的隐含约定，需要在数据结构、常量或调用入口处补充注释，例如某字段由谁写入、谁消费、是否持久化、是否要求顺序稳定、是否可以为空。
- 注释应尽量贴近代码放置：类型注释放在声明上方，方法注释放在方法上方，关键逻辑注释放在分支或代码块前，变量/常量注释放在声明上方。
- 注释要准确维护。修改代码时必须同步更新过期注释；如果发现注释与实现不一致，应优先修正注释或实现，避免留下误导信息。
- 避免无意义的逐行注释，不要解释一眼能看懂的赋值、调用或语法；但当变量名不足以表达业务原因、取值依据、边界条件或副作用时，仍然需要补充注释。

## String Resource Convention

- 所有用户可见字符串必须写入对应模块的 `strings.xml`，代码中通过 `stringResource(...)`、`context.getString(...)` 或 `applicationContext.getString(...)` 引用，禁止在 Kotlin/Java 代码中硬编码用户可见文案。
- 带变量的用户可见文案必须使用字符串资源占位符（如 `%1$d`、`%1$s`），不要用字符串拼接或模板字符串直接拼出中文文案。
- 通知标题/内容、Toast、Dialog、按钮、页面标题、错误提示、进度文本、Intent chooser 标题等都属于用户可见字符串，必须资源化。
- 日志、异常调试信息、数据库状态值、网络参数、文件名规则和内部 key 不属于用户可见字符串，可以保留在代码中；如果它们会展示给用户，则必须改为字符串资源。
- 新增字符串时优先放到使用方所在模块的 `strings.xml`；跨模块复用或 base 模块代码需要使用的文案放到 `base/general/src/main/res/values/strings.xml`。

## Git Convention

- 后续创建 Git commit 时，提交信息必须使用简体中文，简洁概括本次改动的真实范围和目的。
- 提交前必须确认工作区和暂存区范围：先查看 `git status --short` 和 staged diff，只暂存本次任务相关文件，避免混入用户或其他任务的改动；默认不要使用 `git add .`，除非已经确认整个工作区都属于本次提交。
- commit 粒度必须保持单一：一个 commit 只表达一个清晰目的；代码和对应方案文档属于同一行为变更时可以放在同一个 commit；格式化、重构和功能修改不要混在一起，除非格式化是本次改动的必要结果。
- 提交前必须尽量完成相关验证：涉及 Kotlin、Compose 或 Android 代码改动时，优先运行 `./gradlew :app:compileDebugKotlin` 或更贴近改动模块的编译/测试命令；如果因为耗时、环境或权限原因没有验证，必须在最终说明中明确说明。
- commit message 必须使用简体中文，标题用动宾短句直接说明改动对象和目的；范围明确时可以使用 `模块：动作和目的` 的格式，例如 `列表：修复切回时滚动回顶`。
- commit 不能只有一句标题，必须包含正文细则：至少列出关键改动点、已执行的验证命令和结果；如果存在风险、取舍、未验证项或后续注意事项，也必须在正文中说明。
- 禁止擅自改写历史或破坏工作区：不要主动执行 `git reset --hard`、`git checkout --`、`git rebase`、`git commit --amend`、强制推送等可能丢失或改写用户工作的操作；确实需要时必须先明确征得用户同意。
- 提交后必须检查结果：查看 `git status --short` 确认工作区是否干净，并用 `git log -1 --oneline --stat` 核对提交号和文件范围，再向用户汇报。
